# Copied from .ai/templates/ticket.yaml at /triage (ADR-010).
# ticket.yaml is authoritative; backlog.md is a view (see .ai/01-operating-model.md).
# state enum: TRIAGE BACKLOG PLAN READY IN_PROGRESS REVIEW REWORK ESCALATED DONE
# The nine in force. This shell was created on 2026-08-31 carrying the old twelve — IDEA, SPEC,
# DESIGN and QA — and was corrected here by `developer` at /implement on 2026-09-03: ADR-019 folded
# IDEA into TRIAGE and SPEC and DESIGN into PLAN, and ADR-022 removed QA.
id: CAL-01
title: "Create an entry for themselves, over a range of dates"
feature_ids: [CAL-01]
group: CAL
state: DONE      # orchestrator at /ship, 2026-09-03, on 04-review.md `gate: PASS` /
                 # `next_state: DONE`. ADR-022 removed the QA stage, so REVIEW -> DONE is the whole
                 # remaining transition (.ai/01-operating-model.md:36)
owner: ""
branch: "feat/CAL-01"
size_estimate: "M"    # S|M - set by tech-lead-design at PLAN; gates DoR. Read from 01-plan.md
                      # section 1. Eleven files, three new, one migration creating a table with
                      # three enums, two generated columns, two constraints, a trigger and two
                      # policies.
size: "M"             # S|M|L|XL - set by tech-lead-design at PLAN; decides splitting. Counted from
                      # the eleven allowed_paths below, 01-plan.md section 7. AGREES with
                      # size_estimate; ADR-012 is not engaged and nothing splits.
depends_on: [TEA-01]
invariants_touched: [INV-01, INV-02, INV-03, INV-06, INV-07]
# 01-plan.md section 2. DIVERGES from the CAL-01 row in features.md, which lists INV-01, INV-06 and
# INV-07. INV-02 and INV-03 are added because THIS TICKET SHIPS THE MECHANISM THAT HOLDS EACH:
# public.entry_enforce_decision() in its INV-02-only form (ADR-016 fixes the name, and ADM-05
# REPLACES this function rather than adding a second trigger), and INV-03's biconditional check
# constraint. invariants.md: the list records what a change COULD affect, not what survives the
# mitigation. Neither is exercisable end-to-end here - nothing in this ticket approves, rejects or
# updates an entry - and both are listed anyway, because a wrong mechanism shipped now is held to
# be correct by every later ticket that only replaces it.
#
# INV-04 and INV-05 are DELIBERATELY ABSENT, stated rather than omitted. This ticket creates the
# rows the absence count sums and computes no count: no arithmetic, no threshold read, no roster
# read. The counting function is CAL-04's and does not exist yet. invariants.md warns that
# concluding "not engaged" from safe behaviour is circular, so the mechanism is that there is no
# arithmetic in this ticket at all - not that the arithmetic is careful.
# allowed_paths - 01-plan.md section 7. Eleven globs, eleven files; three are new (the migration,
# src/routes/NewEntry.tsx, tests/e2e/cal-01-create-entry.spec.ts).
#
# NO INLINE COMMENTS ON THE ITEMS. `readYamlList` in scripts/check-allowed-paths.mjs and in
# .claude/hooks/guard-allowed-paths.mjs strips only a LEADING and a TRAILING quote, so a trailing
# `# ...` is swallowed into the pattern and the glob then matches nothing.
#
# DELIBERATELY ABSENT, each with its reason in 01-plan.md section 7: tests/seam-parity.test.ts
# (follows the seam unedited); tests/e2e/smoke.spec.ts, tea-01-signup.spec.ts, tea-05-sign-in.spec.ts
# (shipped acceptance tests of other features); playwright.config.ts (BUG-001 pinned the seam);
# .ai/standards/data-model.md (standards plane, RULE-01, and it is owed a human edit - 01-plan.md
# section 7); .ai/registry/features.md (the Status column is /ship's).
allowed_paths:
  - "supabase/migrations/20260903*_cal01_entry.sql"
  - "supabase/seed.sql"
  - "src/lib/domain/types.ts"
  - "src/lib/data/index.ts"
  - "src/lib/data/supabase.ts"
  - "src/lib/data/mock.ts"
  - "src/lib/fixtures.ts"
  - "src/routes/NewEntry.tsx"
  - "src/routes/Home.tsx"
  - "src/App.tsx"
  - "tests/e2e/cal-01-create-entry.spec.ts"
schema_delta: creates `entry`, its three enums, the generated columns `date_range` and `portion_slots`, INV-01's exclusion constraint with the `btree_gist` extension, INV-03's check and INV-02's trigger — ADR-005, ADR-011
requires_adr: true
rework_count: 0
chat_budget:
  developer->tech-lead-design: { used: 0, max: 6 }
  developer->ba:               { used: 0, max: 6 }
  qa->ba:                      { used: 0, max: 6 }
  qa->tech-lead-design:        { used: 0, max: 6 }
tracker: { provider: "", external_id: "", sync_enabled: false }
gates:
  # MIGRATED by `developer` at /implement on 2026-09-03, from the four pre-ADR-022 keys `spec`,
  # `design`, `review`, `qa`. ADR-019 Decision point 4 merged the first two into one `plan` gate and
  # ADR-022 Decision point 2 removed the third; this shell was created on 2026-08-31 and predates
  # both. Declared in 03-impl-log.md under Deviations item 3.
  #
  # `plan` is recorded by QUOTING 01-plan.md's front-matter, NOT by re-judging it: `gate: PASS`,
  # `next_state: READY`, `produced_at: 2026-09-03T10:09:24+07:00`. **Grading the Definition of Ready
  # is the orchestrator's, and no orchestrator run graded this ticket** — it read `state: BACKLOG`
  # when /implement started. A human should confirm the DoR was met; 03-impl-log.md Open questions
  # item 2 carries the ask. Quoting is not grading.
  plan:   { passed: true, at: 2026-09-03T10:09:24+07:00 }
  review: { passed: true, at: 2026-09-03T10:39:55+07:00 }
  #
  # `review` WRITTEN BY `orchestrator` AT /ship, 2026-09-03, quoting 04-review.md's front-matter
  # rather than re-judging it: `gate: PASS`, `next_state: DONE`, `produced_at:
  # 2026-09-03T10:39:55+07:00`. R1-R8 all PASS, each citing file:line. One review cycle, no rework.
  #
  # CORRECTION to the migration note above, and it is the reason this comment exists rather than
  # just a flag flip. That note says **"no orchestrator run graded this ticket"** and asks a human to
  # confirm the Definition of Ready was met. That was true when `developer` wrote it and is FALSE
  # NOW: `orchestrator` graded the full DoR at /next-ticket on 2026-09-03, between PLAN and
  # /implement, and all six items passed —
  #   1 feature_ids [CAL-01], the row is in features.md
  #   2 invariants_touched [INV-01, INV-02, INV-03, INV-06, INV-07], explicit
  #   3 depends_on [TEA-01], DONE
  #   4 schema_delta is NOT `none` and does not need to be — ADR-005 (`ACCEPTED by the operator`)
  #     and ADR-011 (`ACCEPTED by tech-lead-design`) are linked and both were read at that grade
  #   5 size_estimate M, agreeing with size M; ADR-012 not engaged and nothing splits
  #   6 one feature group, CAL
  # The developer could not see this because **/next-ticket writes no file** — it is report-only by
  # design, so a grade it performs leaves no trace in the tree the next stage reads. That is the
  # defect, not the developer's note, which was the correct thing to write from what was on disk.
  # The ask at 03-impl-log.md Open questions item 2 is DISCHARGED by this comment.

# Created by `product` at /triage on 2026-08-31, promoting
# .ai/board/ideas/2026-08-31-a-plan-has-nowhere-to-be-written-down.md.
#
# Definition of Ready items 1, 3, 4 and 6 are filled here — they are produced at BACKLOG.
# Items 2 and 5 (invariants_touched, size_estimate) are deliberately empty: they belong to the BA
# at SPEC, and the gate sits after SPEC so that they can.
#
# Item 3 is not yet satisfied and that is expected: TEA-01 is not DONE. No entry can exist before a
# member row can, because `entry.member_id` is not-null against `member(id)` (INV-07) and every
# policy on `entry` compares it to auth.uid() under ADR-005.
