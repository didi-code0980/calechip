---
ticket: UIE-08
stage: IN_PROGRESS
agent: developer
produced_at: 2026-09-08T21:13:00+07:00
inputs_read:
  - .ai/board/tickets/UIE-08/01-plan.md
  - .ai/board/tickets/UIE-08/ticket.yaml
  - .ai/standards/testing-standards.md
  - .ai/steward/context.md
  - src/routes/YearView.tsx
  - src/index.css
  - src/routes/MonthView.tsx
  - src/lib/data/mock.ts
  - tests/e2e/cal-06-year-view.spec.ts
  - tests/e2e/cal-08-holiday-shading.spec.ts
  - ui-language.json
  - package.json
consulted: []
chat_before_verdict: none
gate: PASS
blocking_reason: ""
next_state: REVIEW
---

# UIE-08 — implementation log

**The contract is `01-plan.md` § 4, not § 1.** ADR-019 merged SPEC and DESIGN into one artifact, so
the template's "design section 1" is § 4 here, and the selector list the template calls "section 6" is
AC-11 in § 2. Both are cited by their real numbers below.

**One file outside the ticket folder changed, and it is the one file `allowed_paths` names.** Twelve
class-string substitutions and one comment — exactly the thirteen edits § 4 enumerates. No line was
changed that § 4 does not list.

## Files touched

| file | created/modified | why | contract item it satisfies |
|------|------------------|-----|----------------------------|
| `src/routes/YearView.tsx` | modified | The only file the conversion needs: all eight tokens already existed, so nothing was owed to `src/index.css` (§ 0, § 7). Twelve framework-default utilities became the semantic ones, and the stale `MONTH_ENTRY_LIMIT` citation became CAL-09's paging bound. | § 4 rows 1–12, and § 4's thirteenth edit |
| `.ai/board/tickets/UIE-08/03-impl-log.md` | created | This log. RULE-16 — the reviewer gets the plan, the diff and this file, and no channel. | n/a, artifact |
| `.ai/board/tickets/UIE-08/ticket.yaml` | modified | `state` to `REVIEW`, plus `owner` and `branch`, and PLAN's gate transcribed from `01-plan.md` front-matter. See *Open questions* 1 — the gate row was left `false` by PLAN, and is transcribed rather than asserted. | n/a, board state |

`src/index.css` was **not** opened, no spec file was opened, and neither
`src/components/Sidebar.tsx` nor `src/routes/WeekView.tsx` was opened. `dist/` exists from a
verification build and is gitignored.

## Contract items

`01-plan.md` § 4's table, row by row, at the line each now occupies. **Line numbers are
post-change**: the comment edit added two lines at `:174`, so every row below it sits two lines lower
than § 4 predicted. **No name on the right-hand side was invented** — RULE-04. All eight utilities are
generated by Tailwind 4's `@theme` from tokens that already existed, and *Verification run* row 5
confirms each against the built stylesheet rather than against recall.

| § 4 item | Implemented at | Notes |
|----------|----------------|-------|
| 1 | `src/routes/YearView.tsx:272` | `year-loading` — `rounded-card bg-card … shadow-soft` |
| 2 | `src/routes/YearView.tsx:283` | `year-not-on-a-team`, same three |
| 3 | `src/routes/YearView.tsx:298` | `year-unavailable`, same three |
| 4 | `src/routes/YearView.tsx:341` | the grid surface. `overflow-x-auto` and `p-4` untouched, so the scroll stays the grid's and not the page's (AC-8) |
| 5 | `src/routes/YearView.tsx:344` | ruler corner, sticky → `bg-card` |
| 6 | `src/routes/YearView.tsx:369` | `Calendar` label, sticky → `bg-card` |
| 7 | `src/routes/YearView.tsx:387` | `status?.nonWorkingReason === "holiday" ? "bg-holiday" : "bg-bg"` — the literal § 4 wrote out in full, so the ternary was not re-derived |
| 8 | `src/routes/YearView.tsx:393` | bridge dot → `bg-ink-2`. **The one pick here, and it is the plan's rather than mine** — § 2 Open question 2 is where to argue with it |
| 9 | `src/routes/YearView.tsx:414` | member-name cell, sticky → `bg-card` |
| 10 | `src/routes/YearView.tsx:447` | `!mark ? "bg-bg" : mark.type === "wfh" ? "bg-wfh" : "bg-pto"` — likewise written out in full by § 4 |
| 11 | `src/routes/YearView.tsx:478` | `Away` label, sticky → `bg-card` |
| 12 | `src/routes/YearView.tsx:510` | `year-holidays-empty`, same three as rows 1–3 |
| the thirteenth, prose | `src/routes/YearView.tsx:174-176` | Now cites `TEAM_ENTRY_PAGE_SIZE` × `TEAM_ENTRY_MAX_PAGES`, verified at `src/lib/data/mock.ts:1246-1270` with the removal recorded at `:37-38`. The sentence's **claim** — no grid at all rather than one missing the entries the read dropped — is unchanged, and `HOLIDAY_LIMIT` two lines below is untouched because it is still correct. |

**Nothing entered the cell loop.** Rows 7, 8 and 10 are literal string swaps inside ternaries that
already existed and already ran per cell. No lookup, no derivation, no class function, no `clsx` call
and no `var()` — utilities only, which is how Tailwind knows a token is used (`src/index.css:87-90`).
The per-cell work is unchanged to the character across roughly 10,950 cells (§ 4; `:332-338`).

## Deviations from the design

`none`.

Recorded because two things could be misread as deviations and are not:

- **The substitution was made globally, and that was measured rather than assumed safe.** `bg-white`
  occurred exactly 9 times and all 9 are § 4 rows; `rounded-2xl` and `shadow-sm` exactly 5 each and
  all are § 4 rows; `bg-slate-100` twice, and § 4 maps both to `bg-bg`. Every other default occurred
  once. The resulting diff is the 13 lines § 4 names and no others — checkable at `git diff -U0`.
- **The header comment at `:28-31` was left alone deliberately.** It records that § *Colour* in
  `.ai/standards/ui-design-system.md` is still `TODO(project)` and that the palette is therefore cited
  to `CLAUDE.md` § *Visual direction*. Re-read after the change: **both clauses are still true** (§ 2
  Open question 1 says the same), so it is history rather than staleness, and § 4 does not list it.

## Invariants

| ID | Still holds because |
|----|---------------------|
| `INV-04` | **AC-12 was run rather than reasoned about.** No `.filter`, no `.length` and no sum entered this file — the diff contains no expression at all, only string literals and one comment. Filled-ness still comes from `absentDatesByMember` and the totals from `absenceCountsFor`, the same single pass, so the divergence INV-04 exists to make detectable stays detectable. The evidence is `cal-06-year-view.spec.ts` AC-4, AC-5 and **AC-9/AC-10 — "the totals are the month grid's numbers, and they match the filled cells"** — passing unedited against this change. That test reads `data-type` and `data-count` and asserts the two agree, which is exactly AC-12's before/after identity. |
| `INV-05` | The tentative treatment was not touched: `opacity-50` at `:449` and the `year-cell-tentative` `sr-only` span at `:459` are byte-identical, and `cal-06-year-view.spec.ts` AC-6/INV-05 passes. **The clause that mattered is the fill's own alpha, and it is checked:** the three fills introduced resolve to `#ffcbaa`, `#a9e2cd` and `#c9bff0` (`src/index.css:168-170`) — flat opaque hexes with no alpha channel — so composed with `opacity-50`, "away" and "settled" stay separable exactly as before. A token carrying alpha is the case this would have failed, and none of the three does. |
| `INV-06` | A half day weighs `0.5` and a one-day-wide cell still cannot show it, so a `0.5` still reads on this screen as a whole day. **That was true before this change and paint does not move it** — the fix would be geometry, which AC-15 forbids, and the diff touches no `h-3`, `h-4`, `rounded-[2px]`, `gap-px` or `columns`. Recorded as looked at, per § 2, rather than concluded away. |

## Verification run

Commands actually executed, in this session, on this tree.

| Command | Exit | Notes |
|---------|------|-------|
| `pnpm exec tsc --noEmit` | 0 | typecheck, per `.ai/standards/testing-standards.md:16` |
| `pnpm exec eslint .` | 0 | lint, `:17`. **AC-14** — nothing reported for this file, no Vietnamese string was added, and `ui-language.json:21` `copyDebt` is still `[]` |
| `pnpm exec vitest run` | 0 | `:18` — 202 passed across 12 files |
| `pnpm exec playwright test tests/e2e/cal-06-year-view.spec.ts tests/e2e/cal-08-holiday-shading.spec.ts` | 0 | `:19` — **23 passed with neither spec file edited**, which makes AC-11 and AC-12 observed rather than promised. `git diff --stat -- tests/` is empty |
| `pnpm exec vite build`, then grep the emitted stylesheet | 0 | **The one failure mode the four commands above cannot see, and the reason this was run:** a `@theme` name that generates no utility produces a silently unstyled cell that typechecks, lints and passes every existing test. All eight are present and resolve to their tokens — `.bg-holiday`→`var(--color-holiday)`, `.bg-pto`, `.bg-wfh`, `.bg-bg`, `.bg-card`, `.bg-ink-2`, `.rounded-card`→`var(--radius-card)` and `.shadow-soft`. Read with `src/index.css:100-171`, that is AC-1 through AC-6 and AC-8 through AC-10 confirmed at the exact `rgb()` values § 2 states |
| `git diff --name-only` subset of `allowed_paths` | yes | `src/routes/YearView.tsx` plus the ticket folder. Nothing else under `src/`; `dist/` is gitignored |

**AC-13 is held by absence, and the absence is checked:** no `bg-overload`, no `bg-rose-*` and no
`bg-gradient-*` appears anywhere in the file, so no element resolves to `rgb(255, 228, 230)` and every
cell keeps `background-image: none`. `seam.getTeam()` is still not called — the import set is
unchanged, which is also § 5's claim.

## Testability contract

AC-11's list, each at the line it now occupies. **Every one still exists under the same name carrying
the same values**, no `data-*` attribute moved, and every `title` string is byte-identical — the diff
contains no `data-testid`, no `data-` attribute and no `title`.

| selector | Exists at |
|----------|-----------|
| `year-grid` | `src/routes/YearView.tsx:342` |
| `year-month-label` (`data-month`) | `src/routes/YearView.tsx:348` |
| `year-daystatus` | `src/routes/YearView.tsx:368` |
| `year-daystatus-cell` (`data-date`, `data-day-status`, `data-bridge`) | `src/routes/YearView.tsx:377` |
| `year-row` (`data-member-id`) | `src/routes/YearView.tsx:409` |
| `year-row-avatar` | `src/routes/YearView.tsx:415` |
| `year-row-name` | `src/routes/YearView.tsx:418` |
| `year-cell` (`data-date`, `data-type`) | `src/routes/YearView.tsx:433` |
| `year-cell-tentative` | `src/routes/YearView.tsx:459` |
| `year-total` (`data-date`, `data-count`) | `src/routes/YearView.tsx:484` |
| `year-loading` | `src/routes/YearView.tsx:270` |
| `year-not-on-a-team` | `src/routes/YearView.tsx:282` |
| `year-sign-in` | `src/routes/YearView.tsx:286` |
| `year-unavailable` | `src/routes/YearView.tsx:296` |
| `year-holidays-empty` | `src/routes/YearView.tsx:509` |

## Open questions

1. **PLAN left `ticket.yaml` at `state: BACKLOG` with `gates.plan.passed: false`, although
   `01-plan.md` front-matter carries `gate: PASS` and `next_state: READY`.** The `## BACKLOG` row in
   `.ai/board/backlog.md` said `BACKLOG` too, and `owner` and `branch` were empty. This command's own
   instruction is to set the ticket to `REVIEW` on PASS, so the row had to be written; **PLAN's gate
   is transcribed from that artifact's front-matter, carrying that artifact's own `produced_at` as the
   timestamp, and is not a developer assertion that the plan passed.** Flagged rather than fixed
   silently, because one stage writing another stage's gate row is exactly the kind of thing R1 should
   see stated. Whether the omission is a defect in `/plan` or in the hand-off is the steward's
   question, not this ticket's.
2. **`oxfmt --check` fails on this file — and it failed identically on `HEAD`'s version**, verified by
   checking the pre-change file out to a temp path and running the same command against it. There is
   no `format` script in `package.json` and no oxfmt config on disk, so unconfigured oxfmt wants
   semicolon-less style and disagrees with the entire codebase rather than with this change. Not a
   gate command — `.ai/standards/testing-standards.md` names four and this is not one — and out of
   scope, but nobody would otherwise notice.
3. **§ 2's four open questions all still stand, and none was resolved here.** The palette still has no
   document behind it (1, and this is the fourth screen to conform to it), the bridge-dot colour is
   still a pick (2), `src/components/Sidebar.tsx:63-64` still carries the three false clauses (3), and
   `src/routes/WeekView.tsx:296` still carries the same stale `MONTH_ENTRY_LIMIT` line this ticket
   corrected in its own file (4). Repeated so a reviewer does not read 4 as already handled.
4. **The asymmetry `ticket.yaml` § 5 and § 8 alternative 1 both name is unchanged by anything found
   here: if ADR-032 is later accepted, this work is thrown away rather than adjusted.** It stayed the
   right trade at implementation time for the reason the plan gives — folding the two together deletes
   nine shipped domain criteria inside a ticket whose stated scope is a palette — and it is restated so
   the reviewer weighs a known cost rather than discovering one.
