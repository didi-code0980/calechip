# Copied from .ai/templates/ticket.yaml by `product` at /triage, 2026-09-22 (ADR-010).
# ticket.yaml is authoritative; backlog.md is a view (see .ai/01-operating-model.md).
# state enum: TRIAGE BACKLOG PLAN READY IN_PROGRESS REVIEW REWORK ESCALATED DONE
id: CAL-12
title: "An admin chooses which team the three calendar screens and the sidebar roster show, read-only"
feature_ids: [CAL-12]
group: CAL
state: BACKLOG
owner: ""
branch: ""
size_estimate: ""     # S|M - set by tech-lead-design at PLAN; gates DoR
size: ""              # S|M|L|XL - set by tech-lead-design at PLAN; decides splitting
depends_on: [CAL-11]
# ^ CAL-11 supplies the cross-team reads this ticket renders. Without them the only way to show
#   another team is a widened table policy, which ADR-039 § Rationale refuses.
invariants_touched: []    # PLAN's (DoR item 2). Left empty at /triage deliberately.
allowed_paths: []
schema_delta: none
# ^ DoR item 4. Every read it needs arrives with CAL-11; this ticket is interface only. If PLAN finds
#   it needs a function CAL-11 did not deliver, that is BLOCKED, not a migration here.
requires_adr: false
# ^ ADR-040 decisions 1, 2 and 4 are the envelope (ACCEPTED by the operator).
rework_count: 0
chat_budget:
  developer->tech-lead-design: { used: 0, max: 6 }
tracker: { provider: "", external_id: "", sync_enabled: false }
gates:
  plan:   { passed: false, at: null }
  review: { passed: false, at: null }

# Provenance: .ai/board/ideas/2026-09-22-an-admin-manages-every-team-and-sees-only-one-calendar.md,
# § Re-triage verdict. DoR item 6: feature_ids is one group, CAL.
#
# No visual reference was attached at /idea or /triage. The layout is tech-lead-design's to
# originate at /plan § 2b (CLAUDE.md § No invention carve-out). /plan must not be handed an image.
#
# The idea's § Out of scope (seven items) and § Assumptions (six items) bound this ticket. The
# assumptions are product's, not the operator's, and are marked so in the idea.
