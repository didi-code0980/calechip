# Copied from .ai/templates/ticket.yaml at /triage (ADR-010).
# ticket.yaml is authoritative; backlog.md is a view (see .ai/01-operating-model.md).
# state enum: IDEA TRIAGE BACKLOG SPEC READY DESIGN IN_PROGRESS REVIEW QA REWORK ESCALATED DONE
id: CAL-06
title: "Year view — one row per member across 365 days"
feature_ids: [CAL-06]
group: CAL
state: DONE      # orchestrator at /ship, 2026-09-04, on 04-review.md `gate: PASS` /
                 # `next_state: DONE`. ADR-022 removed the QA stage, so REVIEW -> DONE is the whole
                 # remaining transition (.ai/01-operating-model.md:36)
owner: ""
branch: "feat/CAL-06"
size_estimate: "M"    # S|M - set by tech-lead-design at PLAN; gates DoR. Read from 01-plan.md
                      # section 1. Eight files, one new, NO migration, NO seam function, NO new read.
                      # Not S: fourteen criteria, a fourth derivation inside INV-04's module, and a
                      # rendering target the brief states in words. Not L: every read and every rule
                      # already exists and the ticket sits entirely above the seam.
size: "M"             # S|M|L|XL - set by tech-lead-design at PLAN; decides splitting. Counted from
                      # the eight allowed_paths below, 01-plan.md section 7. AGREES with
                      # size_estimate; ADR-012 is not engaged. The same eight CAL-05 needed, for the
                      # same reason: CAL-04's boundary rather than this ticket's restraint.
depends_on: [CAL-04, TEA-03]
invariants_touched: [INV-04, INV-05, INV-07]
# 01-plan.md section 2. EXACTLY the three the CAL-06 row in features.md lists; this plan adds none
# and removes none.
#
# INV-04: this view renders TWO things derived from the same numbers - which cells are filled, and
# the per-day totals. 01-plan.md section 4.1 adds a FOURTH DERIVATION FROM THE SAME SINGLE PASS that
# already produces the counts, the avatars and the week rows, so a filled cell and a total cannot
# disagree without `walk` itself being wrong. AC-9 is the one that matters: it compares this screen
# against the MONTH GRID, and a divergence between them is invisible on either alone.
# INV-05: same pass, which never reads `tentative`. AC-6.
# INV-07: entry_select_team and member_select_team, both already shipped. This ticket introduces NO
# READ OF ITS OWN.
#
# INV-06 IS ABSENT AND, UNLIKE ON CAL-05, NOTHING HERE MAKES IT VISIBLE. A year cell is one day wide
# and carries no portion, so a five-day `pm` entry fills five cells exactly as a `full` one does -
# relied on, not chosen, not rendered. CAL-05's row calls itself the only surface where INV-06 is
# visible and that stays true.
#
# INV-01, INV-02, INV-03 are absent and UNREACHABLE: nothing on this surface writes.
# allowed_paths - 01-plan.md section 7. Eight globs, eight files; two are new (src/routes/YearView.tsx
# and the spec).
#
# NO INLINE COMMENTS ON THE ITEMS. `readYamlList` in scripts/check-allowed-paths.mjs and in
# .claude/hooks/guard-allowed-paths.mjs strips only a LEADING and a TRAILING quote, so a trailing
# `# ...` is swallowed into the pattern and the glob then matches nothing.
#
# NO COLLISION WITH OPS-002. Its five remaining copyDebt files are src/components/EntryForm.tsx,
# src/lib/data/mock.ts, src/lib/data/supabase.ts, src/routes/EditEntry.tsx and NewEntry.tsx - none
# appears below. That is also why Open questions item 2 DECLINES the MONTH_ENTRY_LIMIT rename: it
# would put both seam implementations here for no behaviour change.
#
# DELIBERATELY ABSENT, each with its reason in 01-plan.md section 7: src/lib/domain/types.ts (NOTHING
# IS ADDED - the fourth derivation returns a map of existing primitives and no new constant is
# declared; the first calendar ticket for which this file is untouched); _figma/** (§ 2b - the
# feature row says the prototype IS NOT EVIDENCE and copying it reproduces INV-04's failure twice;
# not read, not cited, not copied); src/lib/data/index.ts, supabase.ts, mock.ts (no seam change -
# section 5); tests/e2e/cal-04-month-view.spec.ts AND tests/e2e/cal-05-week-view.spec.ts (THE SAFETY
# NETS for the two one-line header links - both must pass UNEDITED); tests/seam-parity.test.ts and
# tests/ui-language.test.ts; ui-language.json (YearView.tsx is NEW so the rule covers it from its
# first line and it must NEVER be added to copyDebt); src/lib/fixtures.ts and supabase/seed.sql (no
# fixture added - a thirty-member roster is its own decision, Open questions item 1); every
# migration; .ai/standards/rbac-and-security.md (the `Read the member list` row is CONSUMED and its
# marking reported in Open questions item 3, never amended - RULE-01); .ai/registry/features.md.
allowed_paths:
  - "src/lib/data/absence.ts"
  - "src/routes/YearView.tsx"
  - "src/routes/MonthView.tsx"
  - "src/routes/WeekView.tsx"
  - "src/routes/Home.tsx"
  - "src/App.tsx"
  - "tests/absence.test.ts"
  - "tests/e2e/cal-06-year-view.spec.ts"
schema_delta: none
requires_adr: false
rework_count: 0
chat_budget:
  # Migrated 2026-09-04 by `steward`, together with the gates below. `ba` left the loop at ADR-019
  # and `qa` at ADR-022, so three of the four edges this shell carried named roles nobody can
  # dispatch. They are not kept as zeroes — `.ai/templates/ticket.yaml` says an edge nobody can walk
  # is not a budget, it is a row that invites someone to try.
  developer->tech-lead-design: { used: 0, max: 6 }
tracker: { provider: "", external_id: "", sync_enabled: false }
gates:
  # Migrated 2026-09-04 by `steward`. This shell was created at /triage before ADR-019 merged the
  # `spec` and `design` gates into one `plan` gate, and before ADR-022 removed the `qa` stage and
  # its gate — and nothing migrated it, so five earlier tickets each patched their own copy at PLAN
  # or at /ship. Check D14 now fails any ticket that is not DONE whose gate keys disagree with
  # `.ai/templates/ticket.yaml`, so the set cannot drift again unnoticed.
  #
  # No value changes here, only the key set: every gate on an unstarted shell is `passed: false`
  # under either shape.
  plan:   { passed: true, at: "2026-09-04T16:00:27+07:00" }
  review: { passed: true, at: "2026-09-04T16:38:07+07:00" }
  #
  # `review` WRITTEN BY `orchestrator` AT /ship, 2026-09-04, quoted from 04-review.md's front-matter
  # rather than re-judged: `gate: PASS`, `next_state: DONE`, `produced_at: 2026-09-04T16:38:07+07:00`.
  # All eight checks PASS with citations. One implement cycle, no rework.
  #
  # `gates:` NEEDED NO MIGRATION — first ticket in seven to arrive with the correct two-key shape,
  # because the unstarted shells were fixed between CAL-05's ship and this one. CAL-07, CAL-08, the
  # six ADM rows and OPS-002 are all migrated too; the shells still carrying `spec`/`design`/`qa` are
  # all DONE tickets, which ADR-022 Decision point 2 keeps deliberately.
  #
  # NOTE ON `plan` ABOVE. At /next-ticket on 2026-09-04 16:11 it read `passed: false`, which is the
  # convention TEA-05 recorded — the orchestrator writes it together with the Definition of Ready
  # grade. It was set to `true` after that, stamped 16:00:27, the plan's own `produced_at`. The value
  # is honest and the grade had already happened by then, so unlike CAL-04 the flag does not run
  # ahead of what it attests to. Recorded because two tickets in three have now had this flag written
  # by something other than the orchestrator, and nothing in the model says who owns it.
  #
  # THE DEFINITION OF READY WAS GRADED: `orchestrator`, /next-ticket, 2026-09-04, six of six PASS.
  # Item 4 passed outright — `schema_delta` genuinely `none`, third ticket running. /next-ticket
  # writes no file, so this comment is the grade's only record.

# Created by `product` at /triage on 2026-08-31, promoting
# .ai/board/ideas/2026-08-31-the-team-cannot-see-its-own-shape.md.
#
# Definition of Ready items 1, 3, 4 and 6 are filled here — they are produced at BACKLOG.
# Items 2 and 5 (invariants_touched, size_estimate) are deliberately empty: they belong to the BA
# at SPEC, and the gate sits after SPEC so that they can.
#
# Item 3: TEA-03 is named directly and not only through CAL-04, because this is the one view that
# enumerates members with no entries. rbac-and-security.md's `Read the member list` row exists
# because of this layout, and is recorded there as derived rather than decided — so TEA-03's own
# open confirmation propagates to this ticket and to no other CAL row.
#
# Item 4: `schema_delta: none`. If the 365-column read cannot be served by the seam's count
# function, moving that computation into SQL is a second implementation of INV-04 and needs a
# decision at DESIGN — it is not a schema delta this ticket may assume.
