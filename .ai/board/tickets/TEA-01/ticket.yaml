# Template: ticket.yaml. Copy to .ai/board/tickets/<ID>/ticket.yaml.
# ticket.yaml is authoritative; backlog.md is a view (see .ai/01-operating-model.md).
# state enum: IDEA TRIAGE BACKLOG SPEC READY DESIGN IN_PROGRESS REVIEW QA REWORK ESCALATED DONE
id: TEA-01
title: "Sign up and establish the member record"
feature_ids: [TEA-01]
group: TEA
state: DONE
owner: ""
branch: "feat/TEA-01"
size_estimate: "M"    # S|M - set by ba at SPEC; gates DoR
size: "M"             # S|M|L|XL - set by tech-lead-design at DESIGN; decides splitting
depends_on: []        # TEA-01 waits on the provisioning chore specified in 02-design.md Appendix A.
                      # No ID is written here yet: `product` assigns OPS-nnn at /triage (ADR-010) and
                      # inventing one would leave a reference that resolves to nothing (check D1).
                      # The QA gate cannot turn until that ticket is DONE.
invariants_touched: [INV-04, INV-07]
allowed_paths:        # from 02-design.md section 5 (2026-08-31T09:34:46Z). Twelve files = M.
  - "supabase/migrations/*.sql"
  - "supabase/seed.sql"
  - "src/App.tsx"
  - "src/lib/domain/types.ts"
  - "src/lib/data/index.ts"
  - "src/lib/data/supabase.ts"
  - "src/lib/data/mock.ts"
  - "src/lib/fixtures.ts"
  - "src/routes/SignUp.tsx"
  - "tests/seam-parity.test.ts"
  - "tests/permission-model.test.ts"
  - "tests/e2e/tea-01-signup.spec.ts"
schema_delta: creates `allowed_email` and the trigger on `auth.users` — ADR-009
requires_adr: true
rework_count: 0
chat_budget:
  developer->tech-lead-design: { used: 0, max: 6 }
  developer->ba:               { used: 0, max: 6 }
  qa->ba:                      { used: 0, max: 6 }
  qa->tech-lead-design:        { used: 2, max: 6 }   # 99-questions.md, 2026-08-31T15:18:06Z
tracker: { provider: "", external_id: "", sync_enabled: false }
gates:
  spec:   { passed: true, at: 2026-08-31T09:23:25Z }   # 01-story.md revision that passed
  design: { passed: true,  at: 2026-08-31T09:34:46Z }   # amended twice on 2026-08-31: 15:24:54Z
                                                        # answering QA (§6.2-6.4), and 15:35:07Z
                                                        # recording the operator's choice to
                                                        # provision. Both amendments now PASS.
  review: { passed: true,  at: 2026-08-31T15:52:48Z }   # 04-review.md, R1-R9 all PASS. Carries a
                                                        # RULE-13 disclosure: the second pass reused
                                                        # the session that produced the 15:08:58Z
                                                        # verdict. The reviewer stopped and the
                                                        # operator directed it to proceed.
  qa:     { passed: true,  at: 2026-08-31T16:44:10Z }   # 06-test-report.md, PASS BY OPERATOR WAIVER.
                                                        # Nothing was verified between the BLOCKED
                                                        # verdict of 16:36:28Z and this one. Ten of
                                                        # twelve criteria have no test: AC-1, AC-2,
                                                        # AC-3, AC-4, AC-7, AC-8, AC-9, AC-10, AC-11,
                                                        # AC-12 — the first four on their screen half
                                                        # only, the other six on no half. The waiver
                                                        # is `temporary: true`; the operator said
                                                        # "tạm". Reversed by deleting the `waiver:`
                                                        # block in 06-test-report.md, which returns
                                                        # that gate to BLOCKED with no other edit.

# Shell backfilled by the steward on 2026-08-31. `/triage` promoted TEA-01..04 before ADR-010 made
# shell creation part of that command, so these four exist only because the command was incomplete
# when it ran. Every later ticket is created by `/triage` itself.
#
# Definition of Ready items 1, 3, 4 and 6 are filled here — they are produced at BACKLOG.
# Items 2 and 5 (invariants_touched, size_estimate) are deliberately empty: they belong to the BA
# at SPEC, and the gate sits after SPEC so that they can.
