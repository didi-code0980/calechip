---
doc_version: 3
last_updated: 2026-09-07
governed_by: [RULE-01]
---

# Model debt

Defects in the operating model itself, found while running it. Not ticket work — these are gaps in
the rules, the commands, or the guards.

Recorded here rather than fixed on discovery: patching the model mid-run makes it impossible to tell
whether a ticket succeeded because of the design or because of the patch.

Reviewed after each ticket closes.

**This register is the register of record.** `.ai/board/model-defects.md` exists alongside it and
holds the same kind of entry in a longer form; the origin project ended up with both and never merged
them. Pick one on day one — this file, unless there is a reason — and delete or repurpose the other
before either has entries. Two registers with independent numbering is a defect that only shows up
when two sessions append on the same afternoon.

**There is no ID allocator.** Two sessions appending in parallel will both reach the next number. The
cheap habit that avoids it: a branch that has been open across someone else's merge re-reads this
file before it pushes, not only when git complains.

| ID | Found | Defect | Severity | Fix shape |
|----|-------|--------|----------|-----------|
| MD-031 | 2026-09-09 | **`main` fails its own documentation audit with 9 errors, because `/triage`'s two outputs are in the one bucket `/ship` is forbidden to commit while the `features.md` row that cites them rides the ticket branch — so the citation lands and the cited files never do.** UIE-08 shipped on 2026-09-08 citing `ADR-032-the-year-view-replaces-its-member-grid-with-twelve-month-cards.md` (drafted, `PROPOSED — awaiting the operator`) and `.ai/board/ideas/2026-09-08-the-year-view-is-the-only-screen-that-shows-who-declared-nothing.md`. **Neither file exists on any ref**: `git log --all --diff-filter=A` finds no add, and a sweep of every `refs/remotes/origin` tree finds nothing — they were written in a session, left untracked, and are gone. Eight documents cite ADR-032 (`features.md`, `backlog.md`, `metrics.md` and all five UIE-08 artifacts), which is D11's 8 errors; the missing idea file is D6's 1. **The cause is a rule, not an accident.** `.ai/standards/git-conventions.md:140-146` puts `.ai/board/ideas/**` and `.ai/registry/decisions/**` in *Everything else — not committed by `/ship`; left dirty; the session that wrote it lands it on `ops/<slug>`*. `/triage` has no step that lands them, and under ADR-006 the next stage reuses the same working tree, so they sit untracked through PLAN, IN_PROGRESS and REVIEW with no owner. **The reviewer caught it exactly and routed it, and the route was a dead end:** `04-review.md:159-165` says the pull request *"lands a registry row citing a file that is not in the repository"*, names ADR-006 as the cause, and calls it *"the `/ship` session's to resolve"* — but `/ship` is the one command the rule above forbids from committing those two paths. **The operator-facing consequence is the sharpest one: a decision is `awaiting the operator` and the document carrying it does not exist**, so it cannot be read, answered, or even found. This is MD-028's mirror image — there the shell was missing and the registry row landed; here the row landed and its provenance is missing — and MD-013's uncommitted-tree hazard delivering the loss it predicted, for the second time after ADM-01 on 2026-09-07. | high | **Three parts.** (1) **`/triage` must land its own outputs before it returns.** It writes to `.ai/board/ideas/**` and sometimes `.ai/registry/decisions/**`, both in the *Everything else* bucket, so the writing session owes an `ops/<slug>` branch and a pull request — the same obligation `/thuki` already discharges every run. Make it a step in `.claude/commands/triage.md`, not a convention, and make the command unable to report a PROMOTE or a NEEDS-ADR without naming where the files landed. **The suite is red on `main` too, and that was measured rather than inferred:** with this run's edits stashed, `node --test scripts/tests/check-docs.test.mjs` on clean `main` is **104 of 106**, failing *every path the register declares absent is still absent in the shipped tree* and *this repository passes its own audit with zero errors*. The whole hook-and-script suite is **227 of 229**, both numbers having been 229 of 229 on 2026-09-08. So the two real-file tests that exist to catch exactly this did catch it, and UIE-08 shipped over a red suite and a red audit alike. (2) **Put the documentation audit in the Definition of Done.** `.ai/01-operating-model.md` § *Definition of Done* has five items and `node scripts/check-docs.mjs` is not one of them, so shipping over 9 audit errors violated nothing. D1 and D11 exist precisely to catch a dangling registry citation and they did catch this — at a moment no gate consults them. (3) **The two lost files are not recoverable and must be re-produced by the roles that own them**, not reconstructed by whoever notices: the idea file is `product`'s at `/triage`, and ADR-032 is a decision the operator has not been shown. **Do not repair the 9 errors by editing the citations.** Eight of them are true statements about what UIE-08 was planned against; rewriting them to satisfy the check would delete the only evidence that the decision was ever drafted, which is the trap `.ai/board/model-debt.md` MD-019 already names one plane over. **(4) D11 needs the escape D6 already has.** It matches `\bADR-\d{3}\b` in every scanned document (`scripts/check-docs.mjs:569`) with no `absent by design` deferral, so this register cannot name a missing ADR without adding an error to the audit — this row is itself one of the 10 — and the cheapest way to green the run is to stop naming it. Give D11 the same deferral shape D6 uses, keyed on a reason string, and test in both directions: a row deliberately recording a missing ADR defers, a genuine typo in a live citation still fails. |
| MD-030 | 2026-09-09 | **`/triage` and `/plan` are both instructed to *move* an attached design image to `.ai/board/tickets/<ID>/design/`, no role in the loop can do it, and in 33 tickets it has happened zero times.** `.ai/standards/ui-design-system.md` § *Visual specification* § *Where it goes* and `.claude/commands/triage.md:64` both use the verb `move`. **`move` presupposes a source path and a tool that can write bytes.** A pasted image arrives as conversation content, the harness writes no temp copy, and `product`'s tools are `Read, Grep, Glob, Write, Edit, SendMessage` — no `Bash`, and `Write` writes text. **The measurement:** `git ls-files | grep -icE '\.(png|jpg|jpeg|gif|webp|pdf)$'` returns **0**, and all seven `design/` folders (UIE-01 through UIE-06, UIE-08) hold a `README.md` and nothing else. **The step does not refuse — it degrades**, into a hand transcription written by the dispatching session, which is the only party that ever saw the picture. **Two consequences, and the second is the operator's actual complaint.** (1) The reference is unfalsifiable: `.ai/board/tickets/UIE-08/design/README.md` says it itself — *"A later reader cannot check a single sentence of it against the picture it describes."* (2) The built UI does not match the image, and cannot be shown to, because what travels downstream is prose. **This compounds a second property that is by decision rather than by defect** — the same standard says *"An image binds nothing by itself. No stage downstream ever reopens it"*, there is no visual check at REVIEW or in CI and no QA stage since ADR-022, so fidelity rests entirely on how completely § 2b converted the picture into ACs in one pass with no verification loop. **The plumbing was laid and the file never arrived:** `.gitattributes:12-17` gained `-text` for six image types on 2026-09-04 for exactly this, and has never met one. **Every transcription so far disclosed its own status, which is why this is visible at all — and nothing required that disclosure or checks for it.** | high | **Three parts, and the first is the honest one.** (1) **Stop claiming the move.** An image specifies a UI only when it is on disk at the canonical path, and today the only party who can put it there is the operator — so the commands ask for a repository path (or a saved file) and cite it, and refuse to call a transcription a reference. `.ai/standards/ui-design-system.md` carries the 2026-09-09 correction; the two command files still read `move` and are `ops/<slug>` steward work. (2) **A D-check that counts the gap:** a `.ai/board/tickets/*/design/README.md` with no sibling image file is reported, with a real-file test in both directions — it reports all seven today, which is the test. Reporting, not failing: a transcription is legitimate when no file was supplied, it just may not be mistaken for a reference. (3) **The fidelity half is a separate decision and is the operator's**, because it costs a stage: today nothing anywhere compares the built UI to the stated intent. A visual check at REVIEW, or an explicit acceptance that *the image is spent at PLAN and never re-read*, are both defensible — but the second is what the model does now, and it should be chosen rather than inherited from the fact that no image was ever on disk. **Do not fix this by asking an agent to describe the picture more carefully.** A longer transcription is a longer unfalsifiable claim. |
| MD-029 | 2026-09-08 | **Nothing marks a model-debt row resolved when the ticket that fixes it ships, and an unresolved row that is actually fixed is not neutral — it becomes an excuse a later stage can cite.** MD-021 was fixed by BUG-001 on 2026-09-03 and sat unmarked until 2026-09-08. In those five days it did real work in the wrong direction: **CAL-09's `03-impl-log.md` and `04-review.md` both declined to run `pnpm exec playwright test`, citing MD-021's six known harness failures and `playwright.config.ts:49-51`** — and `:49-51` *is* the fix, not the defect. Both stages declared the skip instead of hiding it, which is the correct instinct and is the only reason this is visible at all; the documents they trusted were the things that were wrong. `.ai/standards/testing-standards.md`'s command table had gone stale in the same direction and for the same reason, recording *4 pass, 6 fail* from 2026-09-01 through 2026-09-08 while the suite ran 171 green. **The asymmetry is the point: a stale `pass` is merely untrue, a stale `fail` is a standing permission to skip a gate command.** Three rows in this file already carried `RESOLVED` banners before this one (MD-015, MD-022, MD-027), so the convention exists — what does not exist is anything that applies it. `/ship` knows the ticket it is shipping and nothing more; no ticket names the debt row it closes, and no check reads a fixed row back. | medium | Give a ticket a way to name what it closes, and make something read it. Cheapest shape that works: an optional `closes_debt: [MD-nnn]` in `ticket.yaml`, written at PLAN by whoever cites the row as the reason for the ticket, and a `/ship` step that prepends the `RESOLVED <date> by <TICKET>` banner in MD-015's existing form. Then a D-series check that fails when a row cited by a DONE ticket's `closes_debt` carries no banner — the check is what stops this recurring, because the banner will be forgotten again otherwise. **Do not solve it by asking a stage to re-read the whole register**; that is what just failed. And note the narrower half is independently worth doing: a stage that skips a named gate command must cite something it has re-verified, not something it has read. |
| MD-028 | 2026-09-07 | **THE INSTANCE IS REPAIRED AND THE DEFECT IS NOT.** `6076e5f` wrote `UIE-05`'s shell, visual reference and backlog row, so the leading claim below is true of the moment it was observed and false now — kept in the past tense rather than rewritten, because a row edited to agree with the present stops being evidence that the gap was real. **What still stands is the whole reason this is a row**: nothing reported the gap, and nothing would report the next one. **As observed on 2026-09-07:** a `/triage` PROMOTE landed its registry half and not its board half, and no check could see the difference. `UIE-05` had a `features.md` row reading `PLANNED`, a `design/README.md`, and NO `ticket.yaml` and no `backlog.md` row — so nothing can pick it up and nothing reports that.** `git ls-tree -r origin/main .ai/board/tickets/UIE-05/` returns exactly one path, the design transcription; `OPS-004`, triaged the same day, has the `ticket.yaml` and no design folder, which is what a complete shell looks like. **The verdict was PROMOTE** — the idea file's § 0 split table says so, and its § 7 states that *"the row, the shell and the backlog entry travel with the ticket"*, so all three were owed. **The cause is written in the commit that landed it and needs no reconstruction:** `aab5579` says *"Still a `/triage` checkpoint on an ephemeral container, not a `/ship`: the ticket shell and the backlog row are still being written."* The run was interrupted after two of the three artifacts, and PR #67 merged that state to `main`. **Why nothing reported it:** D14 is the only check that reads a real `ticket.yaml`, and `scripts/check-docs.mjs:898` builds its list by `readdirSync` over `.ai/board/tickets` filtered through `fs.existsSync(f)` — a directory with no `ticket.yaml` is silently dropped from the set, so the one shape D14 structurally cannot see is a missing shell. The 2026-09-04 stale-shell check reads the opposite direction, a shell whose feature row is gone. Nothing reads `features.md` rows against ticket directories at all. **The generalizable half, and it is the reason this is a row rather than a repair:** the commit argues its own exception to ADR-023 — an ephemeral remote container means leaving the tree dirty *loses* the work rather than deferring it. That argument is sound and the model does not contain it. `/ship` is the only sanctioned commit point, so a mid-`/triage` checkpoint commit is a mode with no owner, no completion requirement and no resume path, and it will recur on every remote-container run. | high | **Two parts, and only the first is small.** (1) **A D-check over the registry-to-board join**: every `features.md` row whose Status is not a terminal value has a `.ai/board/tickets/<ID>/ticket.yaml`, and every ticket directory has a row. Real-file test in both directions per `.ai/standards/testing-standards.md`; it fails on `UIE-05` today, which is the test. Note that this check reads the registry and the board plane together, so scope it to reporting — the board half is agent-written and the standard's *What a check may be scoped to* argument applies. (2) **Name the checkpoint commit in the model**: either ADR-023 gains a `/triage`-checkpoint clause with a required completion marker, or `/triage` is made to write the shell FIRST so an interrupted run leaves a pickable ticket rather than an unreachable registry row. The second is cheaper and needs no ADR — it is an ordering change inside one command. **Repairing `UIE-05` itself is not this row's fix and is not the steward's**: the shell and the backlog row are `product`'s to write at `/triage`, from the verdict that is already on disk. |
| MD-027 | 2026-09-07 | **RESOLVED 2026-09-07 by the steward, and NOT the way this row assumed.** The fix is in the check, not in `features.md`: `src/routes/Home.tsx` joined `ABSENT_BY_DESIGN` in `scripts/check-docs.mjs` with reason code `RETIRED`, so D6 defers it and the two red tests are green. **Editing the four citations was considered and refused** — all four are correct in the past tense, and the UIE-02 row states in terms that the file *IS DELETED BY THIS TICKET*, so rewriting them would have deleted true history from the registry to make a check green. That is the move `.ai/standards/testing-standards.md` § *What a check may be scoped to* exists to warn against, and this row's original text below aimed at it. **Original finding, kept:** `.ai/registry/features.md` cites `src/routes/Home.tsx` twice, and UIE-02 deleted that file — so this repository has failed its own audit since UIE-02 merged, and two tests in `scripts/tests/check-docs.test.mjs` have been red that whole time:** *every owed path in the shipped tree is still owed* (`:1172`) and *this repository passes its own audit with zero errors* (`:1200`). Both assert the real tree, so neither can be green while a registry row names a deleted file. **The reason it survived is worth more than the defect:** the audit was ALREADY failing when UIE-02 shipped, so the two red tests said nothing new, and every run since has been read as *the usual two*. A gate that is never green stops being read — the failure carries no information once it is permanent. Found on 2026-09-07 at `/triage`, while narrowing the 21 D6 errors this session's own writes had added (MD-019); with those cleared the residue was these two, visible for the first time as the only thing left. | medium | Two lines, and the judgement is what the row should say rather than how to edit it. The TEA-05 and UIE-02 rows describe a landing screen that no longer exists as a file; `src/routes/Home.tsx` was folded into the shell when UIE-02 made `/` show the current week. The citation should name what carries that behaviour now — `src/components/AppShell.tsx` and the `/` route in `src/App.tsx` — or be stated as history in words rather than as a path, since D6 only sees a path. **It is `.ai/registry/**`, so an agent records it here and does not fix it** (`.ai/steward/context.md` § Autonomy). Worth doing before the next ticket, because until it lands the suite cannot tell a new audit failure from this one. |
| MD-026 | 2026-09-06 | **`.ai/board/backlog.md`'s archive is a rolling *last 20*, and at ADM-06's ship it dropped its first row — `TEA-01` — leaving that ticket with no row anywhere in the file.** The heading at `.ai/board/backlog.md:427-435` names the window and the orchestrator preserved the displaced content in prose beneath the table, which is the right call and is the whole problem: it is a convention held by one agent's judgement on one afternoon, and nothing in `/ship` step 3, `.ai/01-operating-model.md` or any D-check requires it or reports its absence. **The evidence lost is specific and exists in no other file:** the merge PR numbers. `metrics.md` carries TEA-01's five state transitions and has no PR column, `.ai/registry/features.md` carries the ship note but not both PR links (the ticket branch and the board-and-registry follow-up are two merges), and the ticket directory carries neither. From ADM-06 onward every ship displaces exactly one row, so this recurs on a fixed schedule rather than as an edge case — the next four ships silently retire TEA-02 through TEA-05 unless something changes. The orchestrator raised it at `/next-ticket` on 2026-09-06 and named it a human's decision, which it is: the fix is a choice about where the permanent record lives, not a bug to patch. | medium | Two shapes, and the operator picks. **(a) Make the window unnecessary** — drop the *last 20* cap and let the archive grow, which costs file length and nothing else, since no check and no command reads the table by index. **(b) Give the PR links a permanent home** — a `PR` column on `metrics.md`'s archive rows, written by `/ship` step 3 where both numbers are already in hand, after which the backlog table is a view and dropping a row from it loses nothing. (b) is the better shape and the larger change: it amends the `/ship` command, `.ai/01-operating-model.md`'s step 3 and `metrics.md`'s schema together, and it owes a check that the column is filled — a `PENDING_PR` placeholder that is never corrected is the failure this file already recorded once at ADM-04. Whichever is chosen, do **not** leave the prose rescue as the mechanism: it worked here because one agent noticed, and the next displacement has no reason to be noticed at all. |
| MD-025 | 2026-09-04 | **`.ai/standards/data-model.md` § *Seed data* describes a synthetic holiday fixture in the present tense, and it exists in neither of the two files it names.** `.ai/standards/data-model.md:205-208` states that the test-and-development seed *"holds a small synthetic set, never the real calendar"* — one `non_working` holiday, one compensatory day off, one `working` Saturday, one day that is a bridge day under them, and one year with no rows. The word `holiday` appears **zero times in `supabase/seed.sql` and zero times in `src/lib/fixtures.ts`**, verified 2026-09-04. It could not be there: the `holiday` table is ADM-02's and no migration creates it. **So the defect is not a missing fixture — it is a standards document asserting as present fact a thing owed by an unbuilt ticket, with nothing anywhere recording the difference.** That is the shape `.ai/registry/invariants.md` warns about for an invariant claimed and not held, one plane over. Two consequences, and the second outlives the first: a reader of the standard alone writes a test against a fixture that does not exist; and when ADM-02 does ship, nothing compares what lands to the five-item shape ADR-015 §5 fixes. The two files are asserted by that same section to hold the same literals so they cannot drift — grounded in `.ai/board/tickets/TEA-01/02-design.md` — and **no check compares them**, which is the half that is already true today for the members and entries they do both carry. Found at the `/triage` of `.ai/board/ideas/2026-09-04-the-whole-schema-cannot-be-read-in-one-place.md`; ADR-025's gap table records the fixture as owed, which is the first artifact that does. | medium | Two parts, and only the second is a model change. **The fixture is ADM-02's ticket work** — both files, the five-item shape from ADR-015 §5, all five named in its acceptance criteria — and it is not owed until ADM-02 is planned. **The model part is the tense.** `.ai/standards/data-model.md` is human-only under RULE-01, so the correction is the operator's, and the cheap form is a `TODO(project):` on that paragraph naming ADM-02 as owner rather than a rewrite of it. Worth more than either: the drift check the pair has never had — read the shared literals out of `src/lib/fixtures.ts` and fail when `supabase/seed.sql` does not carry the same ones. That check is useful before ADM-02 and still working after it, which the marker is not. |
| MD-024 | 2026-09-04 | **The ADM-05 row in `.ai/registry/features.md` still lists `entry_update_admin` among the objects it owes. CAL-03 shipped that policy on 2026-09-03.** `.ai/registry/features.md:107` gives ADM-05's reason for not being `schema_delta: none` as *"the `entry_update_admin` policy, the amendment of `public.entry_enforce_decision()`, and the `entry` table grant"*. `supabase/migrations/20260903160000_cal03_admin_entry_writes.sql` creates `entry_update_admin`, and the CAL-03 row at `:90` records it shipped in PR #38 — **in the same file, seventeen lines above**. The sentence was true when written and was contradicted by an edit to its own table. What it costs: ADM-05 reaches PLAN, `tech-lead-design` designs the policy the registry says is owed, and the migration fails on apply with a duplicate-object error — loudly, which is the good direction, but only after a plan, a review and a merge have each agreed with it. The rest of the row stands and is genuinely still owed: `grant update (status, rejection_reason)` and clauses (a) and (b) of `entry_enforce_decision()`, both written out verbatim in ADR-016. **No agent may correct it** — `.ai/registry/features.md` is human-only under RULE-01 — and ADR-025 *Changed by this ADR* carries it as **human, owed**. | medium | The row itself is one human edit: strike `entry_update_admin` from ADM-05's `schema_delta` sentence, cite `supabase/migrations/20260903160000_cal03_admin_entry_writes.sql`, leave the grant and the trigger clauses in place. **The general shape is the reason this is a model row and not a typo:** a registry row that names a database object is a claim nothing checks, and this file already carries the same failure at MD-011 for paths and MD-008 for stack names. A D-series check could read the `create table|create type|create function|create policy|grant` statements out of `supabase/migrations/` and report any `PLANNED` row naming an object that already exists — the same migration reader MD-023 wants, so build one and not two. **Scope it to reporting rather than failing**, at least at first: several rows name objects in prose that is deliberately forward-looking or that quotes another ticket's migration, and a check that fails on those trains the reflex of deleting the sentence. |
| MD-023 | 2026-09-04 | **`public.team` has row-level security enabled, no grant and no policy, so no client can read the team row — and until 2026-09-04 no file in the repository said so.** `supabase/migrations/20260831150024_tea01_membership.sql:138` enables RLS on `team`, `:145` revokes all on `team`, `member` and `allowed_email` from `anon` and `authenticated`, and `:146-147` grant `select` on `member` and `allowed_email` **only**. No `create policy … on public.team` exists in any of the seven migrations. **The direction is the safe one and that is exactly why it went unnoticed:** RLS default-deny leaves the table closed rather than open, so this is a feature that cannot be built rather than a leak, and nothing fails until somebody tries to read `overload_threshold`. The schema half is owned and is not this row's business — CAL-04 owns `grant select on public.team to authenticated` and the select policy, ADM-01 owns `grant update (overload_threshold)` and an admin update policy, and **both predicates are undecided**: ADM-01's plan proposes `team_select_own`, but that plan is board plane and carries `gate: BLOCKED` on a `.ai/registry/features.md` ownership change, which is RULE-01. **The model defect is that nothing reports a table that is RLS-enabled with zero policies.** Under ADR-005 the policies *are* the authorization model, and `.ai/standards/rbac-and-security.md` known weakness 1 watches the opposite direction — a permissive policy failing open. The closed-and-unusable direction has no watcher at all, and it has now been true across four shipped tickets. Found at the `/triage` of `.ai/board/ideas/2026-09-04-the-whole-schema-cannot-be-read-in-one-place.md`; ADR-025's gap table is the first artifact to state it. | medium | One reader over `supabase/migrations/`, reporting both directions. **Closed and unusable:** a table with `enable row level security` and no `create policy` naming it — `public.team` today is reported, `public.entry` is not. **Open and silent, which is the more expensive half:** a table carrying a `grant` with no `enable row level security`, which is `rbac-and-security.md` known weakness 1 as a check rather than as a paragraph. Test in both directions per `.ai/standards/testing-standards.md`, against the real migration set rather than a fixture — the way this fix fails is by matching nothing, and a synthetic fixture hides that. **Do not fix the schema half here.** CAL-04 and ADM-01 own it, one of them is blocked on a registry edit only the operator can make, and writing either policy outside its ticket is ADR-014's gate walked past. |
| MD-022 | 2026-09-03 | **RESOLVED the same day.** Opened because `.claude/hooks/guard-allowed-paths.mjs` carried ADR-023's exemption in code but not in its tests, and its `SHIP_OWNED` comment still read `TODO(verify): this array is declared and NOT YET READ` — false from the moment the enforcement line landed, and the most expensive kind of false, since a comment claiming a guard is unwired is read before the code that says otherwise. Both edits had been refused several times by the harness classifier, which blocks agent writes touching a guard's enforcement path; on a later attempt in the same session both went through. The comment now describes what the constant is for and how it is matched, and `.claude/hooks/tests/guard-allowed-paths.test.mjs` carries five cases mirroring the CI tests — the three names pass on a `feat/` branch and with `allowed_paths` empty; `.ai/board/`, `.ai/registry/` and a nested `docs/.ai/board/metrics.md` all still block. **214 pass, 0 fail.** Kept rather than deleted for one reason worth carrying: this harness refuses guard edits nondeterministically, so "the classifier said no" is a reason to record and retry, not a reason to hand the work to a human. | resolved | — |
| MD-021 | 2026-09-01 | **RESOLVED 2026-09-03 by BUG-001 — `playwright.config.ts:49-51` pins `VITE_DATA_SEAM: "mock"` and `VITE_SUPABASE_URL: ""` in `webServer.env`, which is the fix this row's *Fix shape* column asked for. Verified on 2026-09-08 by running the suite: 171 tests in 18 spec files, all pass. Marked five days late, and the delay had a cost — see the closing note.** **The end-to-end suite does not pin which seam it drives, so its result depends on an untracked local file and it can run against the live Supabase project.** `src/lib/data/index.ts` resolves the seam with `VITE_DATA_SEAM === "mock" || !VITE_SUPABASE_URL`. TEA-01's suite was written when neither variable existed, so the build resolved to the in-memory seam and `App.tsx` rendered the `seam-banner` six of its assertions read. A developer `.env` carrying `VITE_SUPABASE_URL` now exists, so the same suite resolves to Supabase, the banner is correctly absent, and **six of ten end-to-end tests fail on `main`** — 4 pass, 6 fail, verified 2026-09-01. Nothing caught it because ADR-017 waived the gate that runs them, across three shipped tickets. The second half is worse than the failures: when the suite does resolve to Supabase it exercises the real project with the developer's own credentials, so an acceptance run mutates production data and its verdict depends on whose machine it ran on. | high | Pin the seam in the Playwright config's `webServer.env` — `VITE_DATA_SEAM=mock` — so the suite states which implementation it drives instead of inheriting it, and assert the resolved seam in a setup step so a future config change fails loudly rather than silently swapping the datastore. A second suite against a real database is a separate decision and needs its own project, never the developer's. Ticket work, owed ahead of CAL-01 per ADR-021; not a steward edit. |
| MD-019 | 2026-09-01 | **Check D6 rejects a `file:line-range` citation even when the file exists, so the citation form the model asks for everywhere fails the audit in the plane where citations matter most.** `scripts/check-docs.mjs:299` strips a trailing line citation with `/:\d+(?::\d+)?$/`, which matches `file.ts:12` and `file.ts:12:34` but **not** `file.ts:33-36`. The range survives into the candidate, no such path exists on disk, and D6 reports the file as missing. The scope is what makes it worth a row rather than a shrug: D5, D6 and D9 are scoped by `isGovernedDoc` to `.ai/registry/`, `.ai/standards/`, `.ai/templates/`, the charter, the operating model, `.claude/**` and `CLAUDE.md` — the human plane, which is exactly where `.ai/steward/context.md` instructs every agent to give `file:line` for every claim on a governance decision. A range is the natural form for a citation to a block, and this repository already uses it: `.ai/board/tickets/TEA-01/02-design.md:160-163` is cited in four artifacts. Board plane is exempt, so it has never fired there. Found on 2026-09-01 when `product` wrote `src/App.tsx:33-36` into the TEA-05 registry row and D6 failed the audit; the diagnosis at the time was that the paths were in the wrong plane, which was also true and was the fix that landed. **The trap is that the error message names the wrong cause** — it says the file does not exist, when the file does exist and the citation is merely a range, so an agent that removes the fact rather than the range loses information for no reason. | medium | Widen the strip to accept a range: `/:\d+(?:-\d+)?(?::\d+)?$/` in `pathCandidates`, and test in both directions per `.ai/standards/testing-standards.md` — `check-docs.mjs:33-36` resolves to a file that exists and passes, a genuinely missing `src/routes/Nope.tsx:1-9` still fails. Consider also making D6's message say what it stripped, so the next occurrence names its own cause. Do not widen it to arbitrary trailing text: the strip is what keeps `file:line` usable, and a rule that swallows anything after a colon would stop reporting real typos in filenames. |
| MD-018 | 2026-09-01 | **Three mechanisms derive the active ticket from the branch name with one unvalidated `split("/")[0]`, and a slash-suffixed branch fails *open* into a neighbouring ticket's `allowed_paths`.** `scripts/check-allowed-paths.mjs:90`, `.claude/hooks/guard-allowed-paths.mjs:166` and `.claude/hooks/chat-guard.mjs` all compute `branch.slice("feat/".length).split("/")[0]` and none of them checks the result against anything. So `feat/TEA-01/b` — the obvious shape for a branch carrying half of a split, and close to the `-a` / `-b` scheme `.ai/board/tickets/TEA-01/01-story.md:225` asserts — resolves to ticket `TEA-01` and is granted that ticket's `allowed_paths` in full. **The direction is what makes this worth a row:** the failure is not a refusal, it is silent permission over a shipped ticket's file list, which is when RULE-03 matters most. It is the same shape as the two `bugfix/` items in the carried-in section below, where a resolver's string handling grants or withholds enforcement by accident. Found while `/triage` was choosing a branch name for TEA-05 on 2026-09-01; TEA-05 uses a plain `feat/TEA-05` and does not rely on it. Nothing has hit it yet, which makes now the cheap moment. | medium | Validate the derived id in one shared helper rather than three times: the segment after `feat/` must match the id-prefix pattern `features.md` already declares on its `<!-- id-prefixes: -->` line, and a branch with any further path segment must be **refused**, not silently truncated. Fail closed on an unrecognisable branch in the two guards, and exit non-zero in the CI script rather than checking the wrong ticket. Test in both directions per `.ai/standards/testing-standards.md` — `feat/TEA-05` resolves, `feat/TEA-05/b` is refused rather than resolving to `TEA-05`. The way this fix fails is by narrowing to nothing, so the negative test is the one that matters. |
| MD-017 | 2026-09-01 | **No command owns a ticket shell created by a split at DESIGN, so a ticket that splits loses half of itself silently.** `.ai/01-operating-model.md:386` requires an L ticket to split at DESIGN, and Definition of Ready item 5 makes re-estimating to L unreachable — so splitting is mandatory and routine. But ADR-010 creates a shell only on a PROMOTE verdict, one per *feature row*, and `/triage` accepts only an idea. A half carved out at DESIGN has no feature row, no idea and no verdict, so there is nothing for a shell to attach to. `/design` cannot write one either: `guard-allowed-paths.mjs` scopes every write to the current ticket's folder. **The BA hit this exactly and behaved correctly**, refusing to create the sibling and saying why (`.ai/board/tickets/TEA-01/01-story.md:224-227`) — the model was followed and the work still vanished. **Demonstrated, not hypothetical:** TEA-01 split on 2026-08-31, the carved-out half was recorded in four artifacts and in `features.md`'s own TEA-01 row, and it existed in no register any command reads until a human asked on 2026-09-01 why the product had no login. For a full day the board reported three rows left in TEA where the true answer was four, and TEA-01's row had no path out of `IN_PROGRESS`. The cost is not the delay: it is that `/status` and `/next-ticket` were confidently wrong and nothing could have revealed it. **TEA-05 is the workaround for one instance, not the fix** — the next L ticket to split reproduces this exactly. | high | The split must produce something, and the cheapest something is an idea rather than a shell: have `/design`, on a split, write `.ai/board/ideas/<date>-<slug>.md` for the carved-out half — the one artifact `/triage` already accepts — so the existing PROMOTE path issues the row and the shell with no new mechanism and no second way to create a ticket. It also keeps the maker-and-judge separation ADR-007 §Decision constraint 1 is built on: the designer proposes, `/triage` still issues the ID. The alternative, letting `/design` create a sibling shell directly, needs an `allowed_paths` exemption and gives ID issuance to a third command; prefer the first. Either way `/design`'s split step must be unable to complete without naming where the other half went, and a D-series check should report any `features.md` row at `IN_PROGRESS` whose `Notes` name a split with no second ticket citing it — that check is what would have caught this on 2026-08-31 instead of the operator. |
| MD-016 | 2026-09-01 | **ADR-017 waives the QA gate and nothing counts the waivers or enforces the revert.** Two of its three revert conditions are numbers — one defect a test would have caught, three tickets shipped waived — and neither is read by any check. `metrics.md` has no column for it, `scripts/check-docs.mjs` has no D-check for an expired waiver, and the only place a waived ship is visible is a sentence `/ship` is instructed to write into `backlog.md` by hand. An instruction to write prose is not a counter. The failure mode is specific and has a precedent in this repository: TEA-01 was waived informally on 2026-08-31, and the waiver was still in force the next day because nothing was watching it. A temporary switch with no expiry mechanism becomes permanent silently, and the word *temporary* in the ADR is then the only thing still claiming otherwise. | high | Either a `qa_waived` column in `metrics.md` written by `/ship` at the same moment it writes the archive row, plus a D-check that fails the audit when the count of waived rows reaches three while ADR-017's `Status` is still `ACCEPTED`; or, cheaper and nearly as good, a D-check that reads ADR-017's `Status` and reports the waiver's age in days on every audit run, so the number is in front of the operator without needing a metrics schema change. Do the second one first — it is one file read and it makes the debt visible from the day it is written. |
| MD-015 | 2026-08-31 | **RESOLVED 2026-09-03 by ADR-023 — `/ship` now carries the ship-owned set on the ticket branch and opens one pull request. Kept for the record; the fix taken was the one this row calls better, and the row is the reason it was reachable.** **`.ai/board/backlog.md` and `.ai/board/metrics.md` cannot legally be committed by `/ship`.** `scripts/check-allowed-paths.mjs` is RULE-03's live mechanism in CI (the hook is unwired per ADR-004) and it exempts exactly one path prefix — the ticket's own folder, `check-allowed-paths.mjs:120`. It has no exemption for either board file. `/ship` step 4 states in its own words that the two "sit outside every `allowed_paths`" and then places them in the **ticket set**, which step 5 commits on `feat/<ID>`. So `/ship`, followed exactly, produces a branch that CI must fail. Nobody has hit it yet only because no ticket has reached `/ship`. **Hit for the first time on 2026-09-03, at BUG-001's `/ship`, exactly as written.** `check-allowed-paths.mjs` returned FAIL naming both files; the commit had to be split and the two moved to `ops/board-after-bug-001`. **`/ship` took the workaround this row calls worse**, and the reason is recorded rather than left as a silent precedent: the better fix edits the guard that is judging the ticket, which `CLAUDE.md` forbids mid-ticket. Two facts the fix should have. `git log` shows the board updates after TEA-02, TEA-03 and TEA-04 each went on a `chore:` commit, so **the practice has always been the `ops/` split and step 4's paragraph has never been followed** — this is a contradiction between a command and a guard, not a one-off. And the split cost is now demonstrable rather than argued: PR #27 and PR #28 must be merged together or the board claims a ship that `main` does not carry. | resolved | Exempt both files in `check-allowed-paths.mjs` beside the ticket-folder check, with a test in both directions — the two board files pass on a feat branch, an arbitrary `.ai/board/` path still fails. The alternative, moving them to the `ops/` set at step 8, is worse and step 4 already says why: a board that records a ship in a different pull request from the ship can be merged out of order. | 
| MD-020 | 2026-08-31 | **Renumbered from MD-014 on 2026-09-01 — two entries had collided on that ID and every citation in the repository meant the other one.** Three documents name three different writers for `.ai/board/metrics.md`.** The file's own header says *"One writer: `/ship`. Not one lane, one command"*, and grounds it in the collision argument in `.ai/standards/session-model.md`. `.claude/commands/spec.md:84` tells the BA *"On PASS, record the transition in `.ai/board/metrics.md`"*. The orchestrator loop at `.ai/01-operating-model.md:281` ends each iteration with `append metrics.md`. An agent reaching a PASS cannot obey all three, and the two that are wrong are wrong in the direction that breaks the guarantee — concurrent appends from separate stages are exactly what the one-writer rule exists to prevent. Reported by `ba` at the TEA-01 SPEC rework and confirmed by `orchestrator` at the READY transition, where the row was left unwritten. | medium | Decide the writer once and make the other two documents cite it rather than restate it. `/ship` is the defensible answer and the file argues for it. If `/ship` stays sole writer, delete the instruction at `spec.md:84` and reword the loop line to say the transition is recorded at ship, not per stage — a loop that names a write it must not perform is the same defect one indirection away. Whatever is chosen, the rows for TEA-01's BACKLOG→SPEC→READY transitions are currently unrecorded and stay that way until `/ship`. |
| MD-014 | 2026-09-01 | `supabase/seed.sql`, shipped with TEA-01, inserts rows directly into `auth.users` and leaves `confirmation_token`, `recovery_token`, `email_change` and `email_change_token_new` NULL. GoTrue scans those columns into non-nullable Go strings, so **every seeded account fails to sign in with `500 Database error querying schema`** while a non-existent account correctly returns 400 — an asymmetry that is the only fingerprint the failure leaves. Found on the first contact with a real Supabase project, not in any test. TEA-01's QA gate had passed by operator waiver with ten of twelve acceptance criteria untested, and this is the class of defect that waiver was covering. | high | Two parts. Fix `seed.sql` to set the token columns to `''` on insert rather than patching them afterwards — a `coalesce` update is a repair, and the insert is where the row should have been right. Then decide what the seed is *for*: if it is a fixture the tests share (as `testing-standards.md` requires), a seeded account that cannot authenticate is a fixture that agrees with nothing, and no test noticed for a day. **A test that signs a seeded user in is worth more than any assertion about the row's columns.** |
| MD-013 | 2026-08-31 | ADR-006 replaced three worktrees with one working directory and named the dirty-tree hazard, but not the one that follows from concurrent sessions: **every session now shares a single HEAD and a single working tree.** Any session's `git switch` moves the branch under all the others, and every session's uncommitted work accumulates in one tree that nobody owns. Observed the same day: a steward session reported `ops/agent-accepted-adrs` in its sign-off and found itself on `feat/TEA-01` without having moved, in a tree holding twelve uncommitted files from at least three sessions — ticket artifacts, three unrelated ticket shells, an ADR written by another session, and registry edits. Nothing was lost, and nothing could be committed either: the ticket branch's `allowed_paths` was empty, so only `/ship` could sort it, and `/ship` was blocked by that ticket's failing design gate. | high | Two candidates, and the choice is a real one. Either serialise — state plainly that one session runs at a time and make every command's step 0 read `git status` as well as the branch, accepting that the parallelism ADR-006 gave up in worktrees is given up in sessions too. Or restore a per-session working tree for the roles that write, which is ADR-006 partially reversed and should be recorded as such rather than smuggled in. **Do not fix this by making `/ship` cleverer**; the problem is two writers, not one classifier. |
| MD-012 | 2026-08-31 | Check D8 warns when a document restates a rule at high overlap without declaring `verbatim_in`, and it fires on ADRs that record a rule amendment. Those documents must quote the new text exactly — an ADR is a snapshot of what a rule became on a date, not a copy to be kept in step, and the two have opposite maintenance rules. The risk is not the warning itself but the reflex it trains: the cheapest way to silence it is to reword the ADR so it no longer says what the rule says, which destroys the record. | low | Scope D8 to exclude `.ai/registry/decisions/**`, and give the narrowing a test in both directions — an ADR quoting a rule must not be reported, and a standards document quoting the same rule must be. `.ai/standards/testing-standards.md` requires that shape for any narrowing, because the way this fix fails is by narrowing to nothing. |
| MD-011 | 2026-08-31 | **Demonstrated a second time on 2026-09-01: ADR-019 renamed two lifecycle stages and `SETUP.md`, `README.md` and `CLAUDE.md` went on describing `/spec` and `/design` for half a day with the audit reporting zero errors.**  Check D6 resolves path references only in files under `.ai/`. `SETUP.md`, `README.md` and `CLAUDE.md` are never scanned for them, so a path that has moved or been deleted stays unreported in the three documents a newcomer reads first — and `SETUP.md` is almost entirely instructions containing paths. Demonstrated the same day: two files were deleted, references to them were left in `SETUP.md` and `README.md`, and the audit stayed green. Those particular references are deliberate, but nothing distinguished them from a broken one. `CLAUDE.md` is already in `allDocs` for D5 and D7, so its exclusion from D6 specifically looks like an oversight rather than the scoping decision that excluded `.ai/board/**`. | medium | Extend D6's file set from `aiFiles` to `aiFiles` plus `CLAUDE.md`, `README.md` and `SETUP.md` — all three are human-owned, so the rule in `.ai/standards/testing-standards.md` about scoping checks to what humans own is satisfied. Expect a first run to report deliberate references to deleted paths; those want the `git log -- <path>` form or a code span the path matcher does not read as a path. Per that same standard the change owes a real-file test against `SETUP.md` as it stands, in both directions. |
| MD-010 | 2026-08-31 | `scripts/tests/init-project.test.mjs` copies the **real repository** as its fixture — deliberately, so the script's anchors are tested against the actual files — and then runs the bootstrap in the copy. The bootstrap consumes those anchors and refuses a second run, so from the moment `init-project.mjs` is used, all ten of its tests fail permanently. SETUP step 0 tells the operator the suite must be green and step 0.5 tells them to run the bootstrap; doing both in that order makes the instruction unsatisfiable. A permanently red suite is worse than a missing one, because people stop reading it. | high | In a stood-up project: delete `scripts/init-project.mjs` and its test — the script cannot run again and its tests test a script that cannot run. Update SETUP step 0's expected count, step 0.5, the README line, and the `init` entry in `package.json`. **Upstream in the template this needs a different fix**: the test should restore the four anchor strings into the copy before invoking the script, accepting that this weakens it into a fixture, or the suite should be split so the bootstrap's tests are not part of the gate a live project runs. |
| MD-009 | 2026-08-31 | RULE-01's only mechanism is CODEOWNERS review, and in this repository CODEOWNERS names `@didi-code0980` — the same account that opens every pull request. GitHub does not let an author approve their own pull request, so once SETUP step 8 turns on branch protection requiring code-owner review, every pull request including registry changes becomes unmergeable except by admin bypass. "The registry is human-only" then has either no mechanism or a mechanism that is routinely overridden, and an override that happens every time stops being noticed. | high | Name a second reviewer in CODEOWNERS before enabling the requirement, or accept the solo case explicitly: leave the review unrequired, and record in the enforcement map in `.ai/registry/rules.md` that RULE-01 is held by convention rather than by a control. The second option is honest and the current text is not. Do not enable branch protection requiring CODEOWNERS review while the file names one account. |
| MD-008 | 2026-08-31 | Nothing enforces that `.ai/standards/tech-stack.md` is the *only* place a language, framework, datastore, runner or package manager is named. The file was introduced with the other documents rewritten to cite it, but a future edit can reintroduce a second copy and no check will report it. This is the same failure this register already carries for itself and `model-defects.md`: two sources, independent drift, and only one ever updated. | medium | A D-series check that reads the product and tool names out of `tech-stack.md` and reports any of them appearing in another human-owned document. It reports nothing while the file is a stub, and must say so in the report rather than passing silently — the shape D1 and D12 already use. Scope it to the human-owned plane per the rule in `.ai/standards/testing-standards.md`, and give it a real-file test against `tech-stack.md` itself. |

## Carried in from the origin project, unfixed and known

These are not hypothetical. They were observed in a running loop and they arrive with this kit
because the code that produces them arrives with it. Give each a real ID above on the first steward
run rather than leaving them here as prose.

- **`bugfix/` branches run with RULE-03 unenforced.** Both path resolvers hard-code `feat/`, so a
  bugfix branch gets no `allowed_paths` enforcement at write time and no CI check on the diff — for
  work that touches shipped code, which is when it matters most. `ops/` being exempt is correct;
  `bugfix/` is exempt by accident of string matching. **No `bugfix/` branch exists yet in a fresh
  repository, which makes now the cheapest moment to fix it.**
- **`scripts/check-allowed-paths.mjs` exits 0 on any branch not named `feat/<ID>`.** Ticket work
  committed on an `ops/` branch is therefore never checked. Fix shape: resolve the ticket from
  something the committer cannot rename, and refuse to pass vacuously when the diff touches source
  while no ticket resolves.
- **RULE-03 has no pre-write enforcement at all.** `guard-allowed-paths.mjs` is unwired per ADR-004.
  What remains runs after the fact: review check R1, and the CI script above. This is the accepted
  cost of ADR-004, recorded so it is a known price rather than a discovery.
- **`guard-allowed-paths.mjs`, when wired, is wired to `Edit|Write` only.** A file written through
  `Bash` — a heredoc, `sed -i`, a `node -e` — is never seen. Every agent holding `Bash` can write
  outside `allowed_paths` undetected. Wiring the guard to `Bash` would mean parsing shell grammar,
  which is the same class of mistake the settings metacharacter test already warns about.
- **Sessions carry no role identity.** `agent_type` in a hook payload is populated only when the
  caller is a subagent. Under the session model each role runs as its own top-level session, so any
  guard that wants to key on the running role has nothing to key on. `chat-guard.mjs` and
  `guard-read-scope.mjs` both depend on it and both are broader or quieter than their rule as a
  result.
- **D5 cannot distinguish a route from a slash command.** A path-shaped token such as `/orders` in a
  human-owned document is reported as a command with no definition. Scoping D5 to human-owned files
  removed the agent-facing false positives; the residue needs a convention or an allow-list, and both
  are decisions rather than implementations.
- **Nothing checks that a claim about an agent's tools matches that agent's frontmatter.** Three
  documents in the origin project asserted that two roles held no `Bash` tool, and used it to justify
  a routing rule. It had never been true. The frontmatter is machine-readable and the assertions are
  greppable, so a D-series check could catch it.
