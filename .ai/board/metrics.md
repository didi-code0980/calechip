---
doc_version: 1
last_updated: 2026-08-25
governed_by: [RULE-06, RULE-07, RULE-08, RULE-11, RULE-15]
---

# Metrics

Appended by `orchestrator` at `/ship`. One row per transition, never edited in place — a corrected
row is a new row with a note, because the history of how a ticket moved is the evidence for whether
the loop works.

**One writer: `/ship`.** Not one lane, one command — see *The one surface that still collides* in
`.ai/standards/session-model.md`. A ticket's transitions are recorded when it ships rather than when
it moves, which is what keeps two branches from appending to the same lines.

## Row schema

`| ts | ticket | from | to | agent | rework_count | tokens | wall_clock_s | notes |`

| ts | ticket | from | to | agent | rework_count | tokens | wall_clock_s | notes |
|----|--------|------|----|-------|--------------|--------|--------------|-------|
| 2026-08-31T09:23:25Z | TEA-01 | BACKLOG | SPEC | ba | 0 | — | — | Second revision. The first (09:05:34Z) was routed back by the DESIGN gate; this one carved out the sign-in half, moved AC-6 to it and added AC-13. Not rework (RULE-08). |
| 2026-08-31T09:23:25Z | TEA-01 | SPEC | READY | orchestrator | 0 | — | — | Full DoR, six of six. `size_estimate` M. |
| 2026-08-31T09:34:46Z | TEA-01 | READY | DESIGN | tech-lead-design | 0 | — | — | `size` M against estimate M, twelve files. First design pass (of 09:34:46Z scope) had verdict L and routed back to SPEC; amended twice, 15:24:54Z and 15:35:07Z. |
| 2026-08-31T15:45:55Z | TEA-01 | DESIGN | IN_PROGRESS | developer | 0 | — | — | Ten files. One QA failure routed here at 15:18:06Z and was fixed; per RULE-08 it is the only one that could have incremented, and the routing table put it on `tech-lead-design` instead. |
| 2026-08-31T15:52:48Z | TEA-01 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1-R9 all PASS. **RULE-13 disclosure in front-matter**: the second pass reused the session that produced the 15:08:58Z verdict; the reviewer stopped and the operator directed it to proceed. |
| 2026-08-31T16:44:10Z | TEA-01 | REVIEW | QA | qa | 0 | — | — | **PASS by operator waiver.** Four passes: 15:18:06Z FAIL, 15:59:31Z BLOCKED, 16:36:28Z BLOCKED, 16:44:10Z PASS. Nothing was verified between the third and the fourth. Ten of twelve criteria have no test. |
| 2026-08-31T16:46Z | TEA-01 | QA | DONE | orchestrator | 0 | — | — | Definition of Done met on five of six items. **Item 4, "every AC maps to a named test", is not met and was waived**; `waiver:` in `06-test-report.md` carries the terms and is `temporary: true`. |
| 2026-08-31T16:56:07Z | TEA-02 | BACKLOG | SPEC | ba | 0 | — | — | Full DoR evaluated by orchestrator (six of six). `size_estimate` M. |
| 2026-08-31T16:56:07Z | TEA-02 | SPEC | READY | orchestrator | 0 | — | — | Six of six. `schema_delta` cites ADR-009 / ADR-014. |
| 2026-08-31T17:14:11Z | TEA-02 | READY | DESIGN | tech-lead-design | 0 | — | — | `size` M against estimate M, eleven files. Gate PASS. |
| 2026-08-31T17:26:08Z | TEA-02 | DESIGN | IN_PROGRESS | developer | 0 | — | — | Nine files implemented. Typecheck and lint clean. |
| 2026-09-01T00:33:50Z | TEA-02 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1-R9 all PASS. All contract items implemented, no invariant violated. |
| 2026-09-01T00:38:00Z | TEA-02 | REVIEW | DONE | orchestrator | 0 | — | — | QA gate waived per ADR-017 on operator direct instruction. |
| 2026-08-31T17:53:58Z | TEA-03 | BACKLOG | SPEC | ba | 0 | — | — | Story complete. Invariants INV-04 and INV-07 identified. `size_estimate` S. Gate PASS. |
| 2026-09-01T00:58:00Z | TEA-03 | SPEC | READY | orchestrator | 0 | — | — | Full DoR evaluated by orchestrator (six of six). ADR-018 linked. |
| 2026-09-01T00:58:50Z | TEA-03 | READY | DESIGN | tech-lead-design | 0 | — | — | `size` M against estimate S (11 files, ADR-012). Gate PASS. |
| 2026-09-01T01:16:23Z | TEA-03 | DESIGN | IN_PROGRESS | developer | 0 | — | — | Nine files implemented. Typecheck and lint clean. |
| 2026-09-01T01:20:08Z | TEA-03 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1-R9 all PASS. All contract items implemented, no invariant violated. |
| 2026-09-01T01:27:10Z | TEA-03 | REVIEW | DONE | orchestrator | 0 | — | — | QA gate waived per ADR-017 on operator standing instruction. |
| 2026-09-01T03:40:34Z | TEA-04 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | First ticket planned under ADR-019, which merged SPEC and DESIGN into one PLAN stage. `size_estimate` M and `size` M agree; ADR-012 not engaged. Nine `allowed_paths`. Gate PASS. |
| 2026-09-01T04:30:00Z | TEA-04 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six. Item 4 failed on a missing ADR until ADR-020 was accepted by the operator; `schema_delta` re-read at this transition per ADR-020 §Affected documents, because it had been written against ADR-018 point 3 as it read before the amendment. |
| 2026-09-01T04:38:11Z | TEA-04 | READY | IN_PROGRESS | developer | 0 | — | — | Seven files. The migration is the feature under ADR-005; everything above it is an affordance. |
| 2026-09-01T04:46:50Z | TEA-04 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1-R9 all PASS, every item citing file:line. |
| 2026-09-01T04:48:46Z | TEA-04 | REVIEW | DONE | orchestrator | 0 | — | — | **QA gate WAIVED per ADR-017 — the third, which fires revert condition 2.** TEA-02, TEA-03 and TEA-04 are the three rows that condition counts. Reverting is registry work under RULE-01 and was not done here. |
| 2026-09-03T01:20:02Z | BUG-001 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | First ticket planned under ADR-022, which removed the QA stage. `size_estimate` S and `size` S agree; ADR-012 not engaged. Two `allowed_paths`, one file new. Gate PASS. One `consulted` entry with `resulted_in_amendment: true` — the operator superseded the branch name to `feat/BUG-001`, which turns the RULE-03 path guard back on. |
| 2026-09-03T01:34:00Z | BUG-001 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **Not written to `ticket.yaml` at the time** — `/next-ticket` is report-only and writes no file, so the ticket still read `state: BACKLOG` on disk when the next stage arrived. This row and the `gates:` comment in `ticket.yaml` are that grade's only record. |
| 2026-09-03T01:43:20Z | BUG-001 | READY | REVIEW | developer | 0 | — | — | `/implement` wrote `state: REVIEW` directly. **IN_PROGRESS was never on disk** and no row claims it was; the developer recorded the skip in `ticket.yaml` and in `03-impl-log.md` *Open questions* item 2, and declined to forge `gates.plan.passed` rather than tidying it. Two files: the `webServer` seam pin and a setup-project guard that asserts the served page. |
| 2026-09-03T02:04:23Z | BUG-001 | REVIEW | DONE | orchestrator | 0 | — | — | R1–R8 all PASS, each citing `file:line`; there is no R9 (`.claude/commands/review.md:42`). `gates.plan` and `gates.review` were written here from each artifact's `produced_at`, repairing the bookkeeping the two stages deliberately left. **No `qa` gate and none owed** — ADR-022, and this ticket was planned after 2026-09-01. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped. What stands in for it is evidence in `04-review.md` rather than a `/ship` run — typecheck exit 0, lint exit 0, and `pnpm exec playwright test` 11 passed on a machine carrying a real `.env`; `pnpm exec vitest run` was run by nobody at this ticket. |
| 2026-09-01T07:26:13Z | TEA-05 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree; ADR-012 not engaged. Twelve `allowed_paths`. Gate PASS. The shell predated ADR-019 and carried `spec`/`design` gates; migrated to one `plan` gate at this stage. |
| 2026-09-01T07:26:13Z | TEA-05 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six — the first ticket to reach this gate with no ADR owed. Item 4 had to be earned: the shell made `schema_delta: none` CONDITIONAL and `01-plan.md` section 6 discharges the condition. |
| 2026-09-01T08:14:14Z | TEA-05 | READY | IN_PROGRESS | developer | 0 | — | — | Cycle 1. Eighteen files across the seam, the session hook, three routes and the fixtures. |
| 2026-09-01T08:18:50Z | TEA-05 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | Cycle 1, nine-item checklist. |
| 2026-09-01T08:33:00Z | TEA-05 | REVIEW | REWORK | qa | 0 | — | — | **`06-test-report.md` gate FAIL** — `pnpm exec playwright test` exit 1, 13 failures. Cause named in `blocking_reason` and it is not this ticket: the end-to-end suite was unpinned and drove the live Supabase project, which is BUG-001. The QA stage was removed by ADR-022 two days later; this is the last row any `qa` agent writes. |
| 2026-09-03T02:39:34Z | TEA-05 | REWORK | IN_PROGRESS | developer | 1 | — | — | Cycle 2, the AC-7 fix. BUG-001 had shipped, so the suite could state which seam it drove. Roughly eighty lines inserted above the TEA-05 block in `mock.ts`, which is why cycle 1's citations in both artifacts no longer resolve — recorded in `04-review.md` rather than repaired, per the additive rule. |
| 2026-09-03T02:47:46Z | TEA-05 | IN_PROGRESS | REVIEW | tech-lead-review | 1 | — | — | Cycle 2, fresh session under RULE-13, renumbered to the post-ADR-022 eight-item checklist. R1–R8 all PASS, no findings. All four commands re-run at exit 0, including `playwright test` 21 tests. |
| 2026-09-03T02:57:02Z | TEA-05 | REVIEW | DONE | orchestrator | 1 | — | — | `review` gate written from `04-review.md` cycle 2 `produced_at`. **`qa` left `false` and not waived** — no stage produces it since ADR-022, and its 2026-09-01 FAIL is superseded by its cause shipping. `rework_count` corrected 0 -> 1; nothing had incremented it. **TEA-05 and TEA-01 both go to `DONE` in `features.md`** — `/ship` step 3 read as plural, per this ticket's two `feature_ids`. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is cycle 2's, from 2026-09-03. |
| 2026-09-03T03:09:24Z | CAL-01 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree; ADR-012 not engaged. Eleven `allowed_paths`. Gate PASS. **`schema_delta` is not `none` and does not need to be** — ADR-005 and ADR-011 are linked and both are ACCEPTED; ADR-014 is why a migration creating a constraint, a trigger and a check cannot be `none`. |
| 2026-09-03T03:15:00Z | CAL-01 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **Not written to `ticket.yaml` at the time** — `/next-ticket` is report-only, so `/implement` read `state: BACKLOG` and `developer` correctly recorded in `ticket.yaml` and `03-impl-log.md` that no orchestrator had graded it. That note is discharged in the `gates:` block at ship. Second occurrence of the same gap; BUG-001 was the first. |
| 2026-09-03T03:30:00Z | CAL-01 | READY | IN_PROGRESS | developer | 0 | — | — | **The first migration this board has shipped.** Creates `entry`, three enums, the generated columns `date_range` and `portion_slots`, INV-01's exclusion constraint on `btree_gist`, INV-03's check and INV-02's trigger. Also migrated the pre-ADR-019 `gates:` block, declared as a deviation rather than done silently. |
| 2026-09-03T03:39:55Z | CAL-01 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS, each citing `file:line`. All four verify commands re-run at exit 0, including `playwright test` — 32 passed, this ticket's eleven plus four earlier suites unedited. |
| 2026-09-03T03:46:11Z | CAL-01 | REVIEW | DONE | orchestrator | 0 | — | — | `review` gate written from `04-review.md`'s `produced_at`. **No rework** — one implement cycle, one review. **First ticket whose invariants are enforced by the database rather than by code**: five of seven touched. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's, from the same day. |
| 2026-09-03T04:55:53Z | CAL-02 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree; ADR-012 not engaged. Eleven `allowed_paths`. Gate PASS. **DoR item 4 arrived FAILING and was discharged here by linking, not authoring** — `schema_delta` is not `none`, and PLAN linked ADR-005, ADR-014 and ADR-016, all already approved, rather than writing a fourth decision over ground three already covered. It also corrected `requires_adr` from `false` to `true`. |
| 2026-09-03T05:00:00Z | CAL-02 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **Not written to `ticket.yaml` at the time** — `/next-ticket` is report-only, so the grade left no trace in the tree the next stage reads. Third ship running with this gap; the record is a comment in the `gates:` block. |
| 2026-09-03T06:30:00Z | CAL-02 | READY | IN_PROGRESS | developer | 0 | — | — | The write path on a member's own entry: two RLS policies, a column-scoped update grant that excludes `member_id`, `status` and `rejection_reason`, a delete grant, and a `create or replace` of `entry_enforce_decision()` adding `updated_at`. Above it, `EntryForm.tsx` extracted so NewEntry and EditEntry share one form. |
| 2026-09-03T07:00:23Z | CAL-02 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS, each citing `file:line`. All four verify commands re-run at exit 0, `playwright test` 44 passed. |
| 2026-09-03T07:07:16Z | CAL-02 | REVIEW | DONE | orchestrator | 0 | — | — | **First ship under ADR-023 — one pull request, not two.** No rework. The `gates:` block still carried its four pre-ADR-019 keys and was migrated here; third ticket running, a different role each time, none instructed to. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's, from the same day. |
| 2026-09-03T07:22:53Z | CAL-03 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree. Twelve `allowed_paths`. Gate PASS. **DoR item 4 arrived FAILING and was discharged by linking, not authoring** — ADR-005, ADR-014, ADR-016 and ADR-018, all already approved; `requires_adr` corrected `false` -> `true`. The `schema_delta` field states what the ticket will NOT do — no grant, no insert policy, no change to `entry_enforce_decision()`, no edit to CAL-02's policies — which is what made R5 checkable rather than a judgement. |
| 2026-09-03T07:30:00Z | CAL-03 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **Not written to `ticket.yaml` at the time** — `/next-ticket` is report-only. Fourth ship running with this gap. |
| 2026-09-03T07:45:00Z | CAL-03 | READY | IN_PROGRESS | developer | 0 | — | — | Two RLS policies and the `TeamEntries` route. **The plan work was found on `ops/ui-language-english` at the previous `/next-ticket`**, carried there by a `git switch` on a dirty tree — MD-013, and the hazard `git-conventions.md` names as *the loss* rather than an inconvenience. Nothing was lost; it was moved back to `feat/CAL-03` before this stage committed anything, and `check-allowed-paths.mjs` would not have caught it because it exits 0 on a non-`feat/` branch. |
| 2026-09-03T08:05:22Z | CAL-03 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | All eight checks PASS, each citing `file:line`. All four verify commands re-run at exit 0, `playwright test` 56 passed. |
| 2026-09-03T08:16:40Z | CAL-03 | REVIEW | DONE | orchestrator | 0 | — | — | No rework. `gates:` migrated from the four pre-ADR-019 keys — fourth ticket running, three different roles across them, none instructed to. **Two paths were left dirty on purpose**: `.ai/board/tickets/OPS-001/`, `OPS-002/` and the idea they came from are `product`'s uncommitted `/triage`, and their rows in `backlog.md` and `features.md` were parked as a patch and restored after this commit so that triage ships whole from its own session rather than half-carried here. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's. |
| 2026-09-03T09:29:46Z | OPS-001 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree. Nine `allowed_paths`, `schema_delta: none`. Gate PASS. **Planned from row 12 of `## BACKLOG` while CAL-04 sat at row 1**, and no row was reordered — recorded in `backlog.md` rather than silently accepted, because that file reserves reordering to a human. |
| 2026-09-03T09:35:00Z | OPS-001 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. Item 3 passes on the fact that CAL-03 reached DONE in #38; the shell's own comment still says it fails, written at triage when that was true. **First ticket whose `gates:` block needed no migration** — created 2026-09-03 from the post-ADR-022 template. |
| 2026-09-03T09:45:00Z | OPS-001 | READY | IN_PROGRESS | developer | 0 | — | — | Seven screens translated, plus `tea-05-sign-in.spec.ts` whose assertions read the copy. `copyDebt` in `ui-language.json` went 12 -> 5: the seven files left the § Language exemption list and are now inside the lint rule rather than beside it. |
| 2026-09-03T09:56:32Z | OPS-001 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS, no findings. All four verify commands exit 0: `vitest run` 15 tests with `tests/ui-language.test.ts` passing unedited, `playwright test` 56 passed with only the one suite edited. |
| 2026-09-03T10:02:14Z | OPS-001 | REVIEW | DONE | orchestrator | 0 | — | — | **The first ticket to ship no capability** — copy only, no behaviour change. Four of its five `feature_ids` were already `DONE`. **The fifth, TEA-02, was stale rather than open**: its row said `IN_PROGRESS` and *"PR #13 open"*, but #13 merged 2026-08-31 and nothing updated it for three days. Corrected to `DONE` on the fact of the merge. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's. |
| 2026-09-04T02:40:00Z | CAL-04 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree; twelve `allowed_paths`, annotated *exactly on the split threshold*. Gate PASS. **DoR item 4 discharged by linking, the CAL-03 precedent** — `schema_delta` is not `none` (`grant select on public.team` plus `team_select_own`; ADR-014 carves out nothing for reads), ADR-005 and ADR-014 linked, `requires_adr` corrected `false` -> `true`. |
| 2026-09-04T03:16:00Z | CAL-04 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **`gates.plan` had already been set `true` at 02:52Z, before this grade ran** — twelve minutes after the plan's own `produced_at`. It agrees with the artifact, but it inverts the convention TEA-05 recorded and BUG-001 relied on, where the flag is the orchestrator's to write *with* the grade. Noted in `ticket.yaml`; nothing else records it. |
| 2026-09-04T06:30:00Z | CAL-04 | READY | IN_PROGRESS | developer | 0 | — | — | **INV-04 is built here, not merely touched.** One pure range-shaped function in `src/lib/data/absence.ts`, imported by both seam implementations and reimplemented in neither — the registry row's condition, because two copies of that arithmetic agree until one is edited. Also the `team` select policy and grant, making `overload_threshold` readable for the first time. |
| 2026-09-04T06:34:16Z | CAL-04 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | All eight checks PASS, each citing `file:line`. All four verify commands exit 0: `vitest run` 3 files / 43 tests, `playwright test` 64 passed. |
| 2026-09-04T07:18:26Z | CAL-04 | REVIEW | DONE | orchestrator | 0 | — | — | No rework. **The diff was twelve for twelve against twelve `allowed_paths`** — none unused, none exceeded, on a ticket sized exactly at the split threshold. Unblocks ADM-01 in fact rather than on paper: #45 made its dependency on CAL-04 real two hours earlier, for this policy. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's. |
| 2026-09-04T07:29:10Z | CAL-05 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree; eight `allowed_paths`, real headroom against the split threshold. Gate PASS. **DoR item 4 passed outright** — `schema_delta` genuinely `none`, no ADR owed, the first time since CAL-01 that item 4 did not have to be discharged at PLAN by linking. |
| 2026-09-04T07:38:00Z | CAL-05 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. Writes no file, so `ticket.yaml`'s `gates:` comment is the only record. |
| 2026-09-04T07:55:00Z | CAL-05 | READY | IN_PROGRESS | developer | 0 | — | — | **Extended CAL-04's shared absence function rather than copying it** — `src/lib/data/absence.ts` +74/-1, no second arithmetic. That is the outcome CAL-04's registry row demanded and the one no test catches failing: two copies pass seam-parity, agree, and diverge only when one is edited. `mondayIndex` is duplicated across MonthView and WeekView on purpose, recorded in the impl log rather than lifted, because section 7 withheld that scope. |
| 2026-09-04T08:04:37Z | CAL-05 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS with citations. All four commands exit 0: `vitest run` 3 files / 57 tests, `playwright test` 73. The review also recorded that `check-allowed-paths.mjs` reports `0 changed file(s)` on an unshipped ticket — true and empty, since nothing is committed before /ship — so R1 was performed against the working tree. |
| 2026-09-04T08:22:50Z | CAL-05 | REVIEW | DONE | orchestrator | 0 | — | — | No rework. Eight for eight against eight `allowed_paths`. **`gates:` migrated from the four pre-ADR-019 keys — sixth ticket, still unowned**, and now inconsistent between tickets as well: CAL-04 arrived already migrated with `plan` pre-set, CAL-05 arrived with neither. Every remaining pre-2026-09-01 shell reproduces it. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's. |
| 2026-09-04T09:00:27Z | CAL-06 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree; eight `allowed_paths`. Gate PASS. **DoR item 4 passed outright, third running** — `schema_delta` genuinely `none`. **First shell in seven to arrive with the correct two-key `gates:` block**, the unstarted shells having been migrated between CAL-05's ship and this plan. |
| 2026-09-04T09:11:00Z | CAL-06 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. `gates.plan` read `false` at that moment, per the TEA-05 convention; it was set `true` afterwards, stamped with the plan's own `produced_at`. Honest value, and the grade preceded it — unlike CAL-04, where the flag ran ahead of the grade. Two tickets in three have now had that flag written by something other than the orchestrator and nothing says who owns it. |
| 2026-09-04T09:30:00Z | CAL-06 | READY | IN_PROGRESS | developer | 0 | — | — | **Third view on CAL-04's absence function, extended again rather than copied** — `absence.ts` +46/-0. CAL-04's row argued the per-date return type existed so the year view would need no second path; this is that year view and the claim held. AC-14 refuses a possibly-truncated read rather than under-reporting it, which is the failure mode that matters most across 365 days. |
| 2026-09-04T09:38:07Z | CAL-06 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | All eight checks PASS with citations. All four commands exit 0: `vitest run` 3 files / 71 tests, `playwright test` 84. |
| 2026-09-04T09:52:38Z | CAL-06 | REVIEW | DONE | orchestrator | 0 | — | — | No rework. Eight for eight against eight `allowed_paths`. **No `gates:` migration needed — the six-ticket run of hand-patching is over.** ADM-01 becomes backlog row 1, the first non-CAL row to reach the top and unblocked since #45 and #48. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's. |
| 2026-09-04T17:03:06Z | ADM-01 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | **Second plan pass.** The first, 2026-09-03T03:29:17Z, returned `gate: BLOCKED`: it could not tell whether ADM-01 might keep `grant select on public.team` and `team_select_own`, which `features.md` assigns to CAL-04, and moving that ownership is RULE-01 human-only. Nothing was decided — CAL-04 shipped the statements on 2026-09-04 and the question stopped existing. This pass dropped the select half, narrowed `schema_delta` to the update half, and passed. **No rework charged (RULE-08): the block was upstream of the plan.** |
| 2026-09-04T17:13:00Z | ADM-01 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **Item 4 reversed from FAIL to PASS without authoring an ADR**: the 2026-09-03 pass owed *the admin write path on team* to the OPERATOR rather than an agent, because it would have settled a `features.md` ownership change; a merged migration closed that question and no registry row moved, so what remained was a policy predicate — PLAN's to design, as CAL-04 designed the select half at its own gate. ADR-005 and ADR-014 linked. |
| 2026-09-04T17:27:31Z | ADM-01 | READY | IN_PROGRESS | developer | 0 | — | — | Nine `allowed_paths`, three files created. `src/lib/domain/types.ts` and `src/lib/fixtures.ts` were **checked and not touched** — no new domain type, no new `FailureCode`, and `FIXTURE_TEAM.overloadThreshold` already `0.5`. AC-13 passes against CAL-04's `MonthView.tsx` with no edit, which is the property CAL-04's registry row argued for when it made the absence function range-shaped. |
| 2026-09-04T17:36:57Z | ADM-01 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS with citations. All four commands exit 0: `vitest run` 4 files / 81 tests, `playwright test` 96. The review also recorded that `.claude/commands/review.md:3` and `:70` still read "R1 to R9" and "All nine pass" after ADR-022 renumbered R8/R9 to R7/R8 — stale command text, not a finding against ADM-01, and BUG-001's row already carries it. |
| 2026-09-04T17:43:05Z | ADM-01 | REVIEW | DONE | orchestrator | 0 | — | — | No rework, across a ticket that was planned twice. **First ADM row to ship**, and the row that makes `overload_threshold` writable at all — TEA-01 created the column and revoked every privilege, so `0.5` was the only value the product could hold, and since CAL-04 that default has been shading real days. **`gates.plan` and `gates.review` were both `false` on arrival and written here from the artifacts**, the convention CAL-06's shell states in terms. Worth one line: the `developer` inferred from `gates.plan.passed: false` that no DoR had been graded and wrote that into the shell — it had been, six of six, before `/implement` ran. `/next-ticket` writes no file, so a grade leaves no trace until `/ship` records it. That is CAL-06's unowned-flag gap read from the other side. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's, on the same tree with nothing committed since. |
| 2026-09-04T17:56:58Z | CAL-07 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` S and `size` S agree — **the first `S` since BUG-001**, six `allowed_paths` against the eight or nine every ticket since CAL-01 has carried. Gate PASS. **DoR item 4 passed outright**: `schema_delta` genuinely `none`, verified rather than inherited from triage. |
| 2026-09-04T18:01:00Z | CAL-07 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **Every item outright, nothing discharged by linking** — first time since CAL-06. `invariants_touched` `[INV-04, INV-05, INV-07]` agrees with the CAL-07 row in `features.md` exactly. Writes no file, so `ticket.yaml`'s `gates:` comment is the grade's only record. |
| 2026-09-04T18:18:47Z | CAL-07 | READY | IN_PROGRESS | developer | 0 | — | — | **The call CAL-04's registry row was designed to make possible.** That row required the absence function be pure and take rows, stating in advance that a function fetching its own rows *cannot be called with an unsaved entry* — which is precisely this ticket's job. `src/lib/draft-entry.ts` lifts the draft into an `Entry` and calls `absenceCountsFor` unchanged; `tests/absence.test.ts` and `tests/seam-parity.test.ts` pass unedited. Third consumer, one arithmetic. |
| 2026-09-04T18:27:37Z | CAL-07 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS with citations. All four commands exit 0: `vitest run` 5 files / 108 tests, `playwright test` 108, 13 of them this ticket's. **Two defects in the artifact rather than the work, both worth a line.** `next_state` reads `QA`, a state ADR-022 removed — `.ai/templates/review-report.md` still ships it, and ADM-01's reviewer corrected the same template hours earlier while this one copied it through. And `inputs_read` lists two files while the report cites `src/components/OverloadWarning.tsx:27` and `:121-127`, so it under-reports what it read, against RULE-16's stand-alone requirement. |
| 2026-09-04T18:32:53Z | CAL-07 | REVIEW | DONE | orchestrator | 0 | — | — | No rework. Six for six against six `allowed_paths`. **DONE was written from the gate, not from `next_state`**, which named the retired QA stage; `.ai/01-operating-model.md:36` is authoritative. **The review-report template is now the standing defect**: it produced a state that does not exist, on the second of two consecutive tickets, and only one reviewer caught it — steward work, `.ai/templates/review-report.md`. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's, on the same tree with nothing committed since. |
| 2026-09-05T01:25:57Z | ADM-02 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree; twelve `allowed_paths`, two of them migrations. Gate PASS. **DoR item 4 passed on an ADR linked at /triage, not one discharged at PLAN** — ADR-015, accepted 2026-08-31, which closed `data-model.md` OPEN QUESTIONS items 1 and 2. First ticket in weeks not to owe that work to PLAN. |
| 2026-09-05T01:34:00Z | ADM-02 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. The same run corrected a note this orchestrator had written into `backlog.md` the night before, which told a future planner to read `data-model.md` OPEN QUESTIONS item 1 as a blocker on the first holiday story. It had been struck through since 2026-08-31. Taken from CAL-04's registry row, which predates ADR-015, and not re-read. |
| 2026-09-05T01:46:43Z | ADM-02 | READY | IN_PROGRESS | developer | 0 | — | — | Twelve for twelve. The seed migration is idempotent with `on conflict (date) do nothing` — `do nothing` and not `do update`, so an admin's correction from the real government announcement survives re-application (ADR-024). `supabase/seed.sql` carries four synthetic rows for local work and is deliberately not the real calendar. |
| 2026-09-05T01:53:04Z | ADM-02 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS with citations. **The review declines to rest R1 on `check-allowed-paths.mjs`** and says why: the script prints PASS *and* `0 changed file(s)` pre-ship, because it diffs `origin/main...HEAD` and nothing is committed before `/ship`. It judged the twelve paths against the working tree instead and flagged `03-impl-log.md:44-45` for offering that exit 0 as evidence. Second of three reviews to catch `.ai/templates/review-report.md` still shipping `next_state: QA`, and the first to cite ADM-01's as precedent. |
| 2026-09-05T01:58:41Z | ADM-02 | REVIEW | DONE | orchestrator | 0 | — | — | No rework. **First ship to merge `main` into the ticket branch before writing the board.** `feat/ADM-02` was cut from `2515a84`, one ship before CAL-07 merged as #53, so its `backlog.md` and `features.md` were pre-CAL-07 — writing the transition on that base would have opened a PR reverting a merged ticket's rows. The merge was clean; no incoming file was dirty. **Nothing in `.claude/commands/ship.md` contemplates this, and it recurs whenever two ships run before the first PR merges.** Consequence: REVIEW's exit-0 evidence described a tree that no longer existed, so the three fast commands were re-run here — `tsc` 0, `eslint` 0, `vitest run` 5 files / 110 tests. **`playwright test` was NOT re-run** and remains REVIEW's, on the pre-merge tree; step 1 is skipped by standing operator instruction. |
| 2026-09-05T03:11:08Z | ADM-03 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree; nine `allowed_paths` at production. Gate PASS. DoR item 4 passed on ADR-014 and ADR-015, both linked at `/triage`. **The plan carried a defect it could not see**: §4.5 and §7 asserted `tests/e2e/adm-02-holidays.spec.ts` would pass unedited, reasoning about selector names only. Corrected at 10:45 — see the IN_PROGRESS row. |
| 2026-09-05T03:17:00Z | ADM-03 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket` against the nine-path version. The tenth path arrived at 10:45 and no DoR item is a function of that count, so the grade stands. Writes no file, so `ticket.yaml`'s `gates:` comment is the only record. |
| 2026-09-05T03:44:00Z | ADM-03 | READY | IN_PROGRESS | developer | 0 | — | — | **The first ticket where bounded chat did the job it exists for.** At 10:35 the developer measured `134 passed, 1 failed` and found ADM-02's AC-13 — zero buttons, zero textboxes, zero forms for an admin on `/holidays` — in direct contradiction with five of this ticket's criteria. RULE-03 forbade the edit, so they **stopped and routed it** in `99-questions.md` rather than widening anything; `tech-lead-design` amended §4.5 and §7 at 10:45, scoped to that one retired criterion, `size` 9 → 10, still M. **RULE-08: no rework charged — the defect was upstream.** `chat_budget` `developer->tech-lead-design` 1 of 6, the first non-zero chat count on the board. |
| 2026-09-05T03:52:00Z | ADM-03 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS with citations, R1 judged against the amended ten globs. All four commands exit 0: `vitest run` 6 files / 125 tests, `playwright test` 134 passed / 0 failed. The review additionally re-ran ADM-02's suite alone — 12 passed — to establish that retiring AC-13 cost that file nothing else. |
| 2026-09-05T03:55:37Z | ADM-03 | REVIEW | DONE | orchestrator | 0 | — | — | No rework, one consultation. **`allowed_paths` grew 9 → 10 between the DoR grade and the ship, and that is not the failure it resembles**: `ship.md`'s *"never widen `allowed_paths`"* prohibits `/ship` widening the list to get past its own step 6, whereas here PLAN amended its own plan, before the edit, through the channel the model provides. Indistinguishable from the diff alone, which is why it is written down. Branch base was `95a83d9`, the current `origin/main`, so no merge was needed — unlike ADM-02. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's, on this same tree with nothing committed since. |
| 2026-09-05T09:13:56Z | CAL-08 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree, read off the same fact — three view surfaces and one new module — which the shell says rather than presenting as corroboration. Seven `allowed_paths`, none of them a seam file: section 5 is `none`, so `listHolidays` stays ADM-02's and untouched. Gate PASS. **DoR item 4 is genuinely `none` under ADR-014** — no migration, no policy, no grant — the only one of this idea's three rows that is. |
| 2026-09-05T09:20:00Z | CAL-08 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **RECORDED AT /ship, NOT WHEN IT HAPPENED, AND THE FIELD WAS NEVER WRITTEN**: `/next-ticket` writes no file, and no orchestrator run wrote `state` or `gates.plan`, so the ticket reached `/implement` still reading `BACKLOG` with both gates `false` while `01-plan.md` read `gate: PASS`. Both the developer and the reviewer named it in their artifacts rather than fixing it — 03-impl-log.md § Open questions 1, 04-review.md § Findings outside the gate item 3 — and this ship is where it closed. |
| 2026-09-05T09:43:37Z | CAL-08 | READY | IN_PROGRESS | developer | 0 | — | — | Seven for seven, no path unused and none exceeded. `dayStatusesFor` is one pure function in one shared module beside `absenceCountsFor`, imported by the three views and reimplemented in neither seam file. **The plan's two shipping assumptions were implemented as written** — A1, a bridge day is a run of exactly one working day; A2, the two bounding days are any two non-working days — both recorded as the operator's to overturn under RULE-01, at the cost of one predicate. |
| 2026-09-05T10:02:44Z | CAL-08 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS with citations. All four commands exit 0: `tsc --noEmit`, `eslint .`, `vitest run` 7 files / 148 tests, `playwright test` 145 passed / 0 failed. **The unit suite was additionally run under `TZ=America/Los_Angeles` and `TZ=Asia/Ho_Chi_Minh`, 148 pass each** — the timezone trap the feature row names is the one that passes every test run in Vietnam, so it was tested from outside it. INV-04 held by four independent citations rather than by the empty list alone. |
| 2026-09-05T10:07:58Z | CAL-08 | REVIEW | DONE | orchestrator | 0 | — | — | No rework, no consultation. Branch base was `46217e5`, the current `origin/main`, so no merge was needed — unlike ADM-02. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's, on this same tree with nothing committed since. **Lint does not run on this machine's default Node 18** — `eslint.config.js:6` uses an import attribute — and REVIEW ran it under a Node 22 toolchain installed outside the repository; nothing in the tree pins the version, so the failure presents as a broken config. That is steward or devops work and is left dirty here. |
| 2026-09-05T10:25:45Z | ADM-04 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree at **eleven of M's ceiling of twelve**, and the plan says so rather than presenting the agreement as corroboration — the estimate predates the seam function's design, and a twelfth file would be the signal to split by surface. Gate PASS. **Both `TODO(project):` markers on the registry row were discharged by taking the row's own recommendations**, marked as assumptions with the criteria to revisit named: WFH goes through approval exactly as PTO with a type filter (AC-8), and the default window is `end_date >= today` with past-dated entries behind an explicit filter, no fourth `entry_status` value (AC-6, AC-7). Same pattern ADM-01 used. |
| 2026-09-05T10:33:00Z | ADM-04 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **THIRD TICKET RUNNING WHERE THIS TRANSITION WAS NEVER WRITTEN TO `ticket.yaml`, AND THE CAUSE IS STRUCTURAL RATHER THAN A LAPSE**: no command in `.claude/commands/` owns PLAN -> READY. The loop at `.ai/01-operating-model.md:284-286` has it, `/next-ticket` grades the gate and writes no file by design, and `/ship` is where `state`, `branch` and `gates.plan` are finally recorded. CAL-08 went through `/implement` and `/review` the same way. **Steward work, and it will recur on ADM-05 unless something is given the write.** |
| 2026-09-05T10:54:33Z | ADM-04 | READY | IN_PROGRESS | developer | 0 | — | — | Nine of eleven `allowed_paths` used; `src/lib/fixtures.ts` and `supabase/seed.sql` were claimed and not needed. **The read pages against an exact count rather than capping**, which is the whole of this row: a capped worklist makes pending items silently invisible and therefore never decided, and the badge and the list cannot disagree because there is one query and one number. Page size 50, safely under the 1000-row cap, with a short-page assertion that detects a lowered cap without knowing its value. **No approve control and no reject control** — the registry row's condition, and the reason a read-only queue is exercisable on its own. |
| 2026-09-05T11:16:29Z | ADM-04 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS with citations. All four commands exit 0: `tsc --noEmit`, `eslint .`, `vitest run` 8 files / 166 tests, `playwright test` 155 passed / 0 failed. **The unit suite was run under `TZ=UTC`, `TZ=Asia/Ho_Chi_Minh` and `TZ=America/Los_Angeles`, 166 pass each** — the `end_date >= today` window is a date comparison and the timezone that hides its bugs is the one the team works in. |
| 2026-09-05T11:22:07Z | ADM-04 | REVIEW | DONE | orchestrator | 0 | — | — | No rework, no consultation. **`origin/main` had moved three commits under this branch** — #57 opened the `UIE` group, added ADR-028 and four shells, and rewrote `backlog.md` and `features.md` — so `origin/main` was merged in before the board was written, the second ship to need it after ADM-02. It **fast-forwarded**: the branch carried no commits of its own, so there is no merge commit. **Unlike ADM-02, REVIEW's evidence still describes this tree exactly**: every incoming path was under `.ai/`, no `src/`, `tests/`, `package.json` or lockfile, so the source tree the reviewer ran against is byte-identical. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped. **A `BUG` row is owed** — `TEAM_ENTRY_LIMIT` and `MONTH_ENTRY_LIMIT` are 2000 against a 1000-row cap, so CAL-04 AC-11 is not held; PLAN found it, recorded it and correctly refused to fix it here. |
| 2026-09-05T12:05:27Z | ADM-05 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree at **ten of M's ceiling of twelve** at production, and the plan says which fact each was read off rather than presenting the agreement as corroboration. Gate PASS. **The plan CORRECTED its own shell's `schema_delta`**: the shell was written at triage on 2026-08-31 and named `entry_update_admin`, which CAL-03 shipped on 2026-09-03 — recreating it would have been a drop-and-create against a policy that already exists and is already correct. Both surviving objects were verified against `supabase/migrations/` rather than read off `supabase/db.sql`'s `[OWED]` labels, **five of which are stale**; this is the first ticket whose migration is transcribed from `db.sql`, so the labels are the input and not documentation. |
| 2026-09-05T14:59:00Z | ADM-05 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **FOURTH TICKET RUNNING WHERE THIS TRANSITION WAS NEVER WRITTEN TO `ticket.yaml`, AND ADM-04's ROW ABOVE PREDICTED IT BY NAME** — *"Steward work, and it will recur on ADM-05 unless something is given the write."* It recurred, unchanged: no command in `.claude/commands/` owns PLAN -> READY, `/next-ticket` grades the gate and writes no file by design, and `/ship` is where `state`, `branch` and `gates.plan` are finally recorded. CAL-08 and ADM-04 went through `/implement` and `/review` the same way. A prediction that comes true on schedule is no longer a note in a shell; it is an open defect with no ticket. |
| 2026-09-05T15:34:37Z | ADM-05 | READY | IN_PROGRESS | developer | 0 | — | — | Eleven of eleven `allowed_paths` used. **The second ticket where bounded chat did the job it exists for, and the same shape as ADM-03's**: §7 required `tests/e2e/adm-04-worklist.spec.ts` to pass UNEDITED, and it could not — ADM-04's AC-9 test asserts STRUCTURALLY that the worklist holds no button and no textarea, which is precisely the absence this ticket exists to remove. RULE-03 forbade the four-line fix, so the developer **stopped and routed it** in `99-questions.md` with three options and a paste-ready form of the one they recommended; `tech-lead-design` amended §7, `allowed_paths` 10 → 11, still M against a ceiling of twelve. **RULE-08: no rework charged — the defect was upstream.** `chat_budget` `developer->tech-lead-design` 1 of 6. The edit was bounded to two structural lines: `form` and `input` stay at 0, the four selector-name assertions and the whole AC-15 vocabulary block are byte-identical to what shipped, so ADM-04's criterion is still a criterion. |
| 2026-09-05T15:44:36Z | ADM-05 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS with citations. All four commands exit 0: `tsc --noEmit`, `eslint .`, `vitest run` 9 files / **182 tests**, `playwright test` **160 passed / 0 failed**. Clause order in the migration is the property the whole ticket turns on and it was checked by comparison rather than by reading: clause (c) is **byte-identical** to `20260903143000_cal02_own_entry_writes.sql`'s and still runs LAST, and CAL-02's and CAL-03's shipped suites — an ordinary edit by an owner and by an admin — pass unedited, which is where a reversed order would have gone red first. |
| 2026-09-05T15:54:26Z | ADM-05 | REVIEW | DONE | orchestrator | 0 | — | — | No rework, one consultation, one amendment. Branch base was `abc7a81`, the current `origin/main`, so **no merge was needed** — unlike ADM-02 and ADM-04. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's, on this same tree with nothing committed since, and `origin/main` has not moved under it. **Two stale `PENDING_PR` markers were corrected here on the fact of the merge** — ADM-04's `backlog.md` and `features.md` rows to [#58](https://github.com/didi-code0980/calechip/pull/58), CAL-08's `features.md` row to [#56](https://github.com/didi-code0980/calechip/pull/56) — the same repair OPS-001's ship made to TEA-02. The structural cause is that `/ship` commits before it opens the pull request, so the row it writes can only be made true by a later run coming back for it, and nothing does that automatically. **Two things this ticket found and correctly refused to fix:** `supabase/db.sql`'s five stale `[OWED]` labels, which want a `check-docs.mjs` check that fails a marker naming a `DONE` ticket; and *"who rejected it and when"*, which the ADM-05 feature row names in an AC and the schema cannot store — no `rejected_by` column, and ADR-016 clause (b) nulls `approved_by` on any transition off `approved`, with the ADR's own revert condition 1 closing the option of parking it there. Closing it is a schema change plus a `data-model.md` amendment, both human plane under RULE-01. |
| 2026-09-05T16:10:00Z | ADM-06 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree at **ten of M's ceiling of twelve**, the same count and the same ceiling ADM-05 planned against, and the plan names the fact each was read off rather than presenting the agreement as corroboration. Gate PASS. **DoR item 4 is not `none` and the ADR was already there**: ADR-016 §4 specifies `public.reject_entries(p_ids uuid[], p_reason text)` as `security invoker`, and the plan transcribes the body from `supabase/db.sql:553-566` rather than composing one — the second ticket to treat those `[OWED]` labels as input. **The rejected alternative is the one the feature row argues against at length**: a PATCH with `id=in.(…)` puts 37 bytes per uuid in the query string and meets a proxy's request-line cap as an opaque 414 at exactly the moment an admin clears a backlog. |
| 2026-09-06T06:28:00Z | ADM-06 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **FIFTH TICKET RUNNING WHERE THIS TRANSITION WAS NEVER WRITTEN TO `ticket.yaml`, AND THIS TIME THE GRADE ITSELF HAPPENED AFTER REVIEW** — read the timestamp against the two rows below it, which are earlier. The cause is the structural one ADM-04's row named and ADM-05's row watched come true: no command in `.claude/commands/` owns PLAN -> READY, `/next-ticket` grades the gate and writes no file by design, and `/ship` is where `state`, `branch` and `gates.plan` are finally recorded. The ticket went BACKLOG -> REVIEW in the file while the work went through PLAN, IN_PROGRESS and REVIEW on disk. **Three consecutive tickets have now predicted this in their own metrics row. It is an open defect with no ticket, and prediction is no longer telling anyone anything new.** |
| 2026-09-05T16:35:00Z | ADM-06 | READY | IN_PROGRESS | developer | 0 | — | — | Ten of ten `allowed_paths` used. No consultation and no amendment — the first ADM ticket since ADM-02 to need neither, and § 7's claim that `tests/e2e/adm-05-approve-reject.spec.ts` passes unedited held, which is what the two before it had to route a question about. **Three deviations were declared in the log rather than taken silently**, all three inside the plan's own latitude: the reason label had to move because ADM-05's suite addresses the old string by text and Playwright's strict mode would resolve it to two elements; `outcome` lives on the caller because the bar unmounts during the re-read AC-12 requires; and the submit disables on `busy` alone, since disabling on an empty selection would make AC-4 unreachable through the interface. |
| 2026-09-06T03:30:08Z | ADM-06 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1–R8 all PASS with citations. All four commands exit 0 under Node v22.11.0: `tsc --noEmit`, `eslint .`, `vitest run` **10 files / 195 tests**, `playwright test` **165 passed / 0 failed**. **`security invoker` at `20260905230000_adm06_reject_entries.sql:74` is the line this gate exists to check** — a `security definer` rewrite would evaluate `entry_update_admin` and ADM-05's clause (a) against the owner instead of the caller and turn the feature into an authorization bypass with every test still green. The review also established why AC-11 is a partial result and not an error: `entry_update_admin`'s `with check` constrains only the new row's team and `member_id` is ungranted, so an unadmitted row is **filtered rather than raised**. **It found and reported that R1 cannot be checked by the script at REVIEW time**: `scripts/check-allowed-paths.mjs` diffs `origin/main...HEAD` and the ticket was entirely uncommitted, so the exit 0 the impl log cites as R1 evidence was vacuous over `0 changed file(s)`. R1 was re-verified by hand against the working tree. |
| 2026-09-06T06:31:55Z | ADM-06 | REVIEW | DONE | orchestrator | 0 | — | — | No rework, no consultation, no amendment. Branch base was `66605b9`, the current `origin/main`, so **no merge was needed**. **Definition of Done item 3 was NOT run at `/ship`**: the operator instructed step 1 to be skipped; the exit-0 evidence is REVIEW's, on this same tree with nothing committed since and `origin/main` unmoved under it — the fourth consecutive ship to record item 3 this way. **The `## ARCHIVE (last 20)` window filled here and displaced `TEA-01`**, whose two PR links exist in no other file; the row's content is reproduced under the table and the choice between growing the window and finding a permanent record is the operator's. **The vacuous-`check-allowed-paths` finding closes at this ship rather than being carried**: step 6 runs the same script over a branch that now has a commit, which is the reading the script was written for. |
| 2026-09-07T01:21:08Z | OPS-002 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` M and `size` M agree at **eleven of M's ceiling of twelve** at production, and the plan names the fact each was read off rather than presenting the agreement as corroboration. Gate PASS. **The plan's first act was to retire an instruction in its own shell.** `ticket.yaml` §0 makes this ticket *invalid rather than blocked* if PR #37 never merges, and orders the standard cited as `3ccbd37:.ai/standards/ui-design-system.md` and never bare, because on any other branch the file was a 52-line stub on which `:44-48` resolved to `## Selectors` — a citation that reads as correct while pointing at the wrong section. **#37 merged 2026-09-03T08:15:20Z**, the file is 165 lines on this branch with `## Language` at `:44`, so the plan cites it bare and the hazard is gone. **`allowed_paths` reached eleven files where §1's scope statement named five**, and the four extra ones are not in `copyDebt` and are already English: `TeamEntries.tsx`, `PendingEntries.tsx`, `WeekView.tsx` and `YearView.tsx` each declare their own copy of the type labels. §6 of the shell knew about three copies; three more shipped after it was written, so the tree held **five** declarations and folding only §6's would have left two identical English copies behind. |
| 2026-09-07T01:29:00Z | OPS-002 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **SIXTH TICKET RUNNING WHERE THIS TRANSITION WAS NEVER WRITTEN TO `ticket.yaml`.** ADM-04's row named the cause, ADM-05's watched it come true, ADM-06's said prediction was no longer telling anyone anything new — and it recurred here unchanged: no command in `.claude/commands/` owns PLAN -> READY, `/next-ticket` grades the gate and writes no file by design, and `/ship` is where `state`, `branch` and both gate rows are finally recorded. **One thing IS new and it is a small correction to how this defect has been described.** `ticket.yaml`'s own comment at /implement called the `false` gate *"a DoR grading `/next-ticket` never ran"*. The grading ran, at 08:29 +07, between `/plan` and `/implement`, and item 3 — which §3 of the shell says fails *on purpose* — passed, because CAL-03 has been DONE since 2026-09-03. **What is missing is a writer for the result, not the grading.** Four tickets have now recorded this and no ticket exists for it. |
| 2026-09-07T01:38:00Z | OPS-002 | READY | IN_PROGRESS | developer | 0 | — | — | Twelve of twelve `allowed_paths` used, after the amendment below took it from eleven. **The third ticket where bounded chat did the job it exists for, and the cleanest instance of it yet.** `01-plan.md` AC-10 empties `copyDebt` and AC-12 asserted the language suite would pass "with its 'still has copy to translate' cases vacuous" — but Vitest 4 fails a `describe` whose only members are `it.each([])` with *No test found in suite*, so the two criteria could not both hold. `tests/ui-language.test.ts` was not in `allowed_paths`, so RULE-03 put the one file that resolves it out of reach. **The developer stopped rather than reaching for it**, wrote `99-questions.md` at 08:38 with the failing output and a paste-ready form of the fix, and routed it. `tech-lead-design` confirmed the error was the plan's — §1's *out of scope* for a lint rule had been carried too far into §7 — added the file, and restated AC-12 with the required suite shape. **RULE-08: no rework charged, the defect was upstream** (`.ai/01-operating-model.md:145`). `chat_budget` `developer->tech-lead-design` 1 of 6. `allowed_paths` eleven -> twelve, **which is M's ceiling exactly, with no headroom left**. |
| 2026-09-07T01:59:52Z | OPS-002 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | R1-R8 all PASS with citations. `pnpm exec tsc --noEmit` exit 0, `pnpm exec eslint .` exit 0, `pnpm exec vitest run` exit 0 with **186 tests in 10 files**, `node scripts/check-docs.mjs` exit 0. **`pnpm exec playwright test` was NOT run at this gate** — the first review since the four-command convention began that does not carry an end-to-end result, and the reason Definition of Done item 3 is recorded as unverified at this ship. **The gate's most useful finding is a mechanical one AC-6 turns on:** the multiset of `code` literals in both seam implementations was extracted from `git show HEAD:<file>` and from the working tree and compared, rather than read — identical on both sides, so no caller's branch condition moved while every `message` beside it changed. **R6 was established structurally rather than by reading**: a diff of `src/` with every string literal removed leaves only map deletions, import lines and one member-expression substitution — no conditional, guard, policy or query changed, and `git status --porcelain supabase/` is empty. **It also declined to take `scripts/check-allowed-paths.mjs`'s exit 0 as R1 evidence**, on the same finding ADM-06's review made: the script diffs `origin/main...HEAD` over an uncommitted tree and passed over `0 changed file(s)`. R1 was re-verified by hand. |
| 2026-09-07T02:07:38Z | OPS-002 | REVIEW | DONE | orchestrator | 0 | — | — | No rework, one consultation, one amendment. Branch base was `3dea4bd`, the current `origin/main`, so **no merge was needed**. **Definition of Done item 3 was NOT run at `/ship` and this time it is NOT fully covered by REVIEW either** — the operator instructed step 1 to be skipped, as on the four ships before it, but REVIEW ran three of the four commands and **`playwright test` was run at no stage of this ticket**. Every earlier ship recording item 3 this way had a green end-to-end run behind it; this one does not, and the risk is named rather than absorbed: this ticket rewrites user-visible strings, and `.ai/standards/testing-standards.md` addresses elements by `getByTestId` **but not everywhere** — the review found three e2e files addressing `"Đã duyệt"` as a person's NAME, which is seed content and correctly unchanged, and that same reading is what an assertion on translated interface copy would need. **`.ai/registry/features.md` was not written at all**, the first ship with no registry write since the column got a writer at TEA-01: all seven `feature_ids` were already `DONE` and there was nothing to correct — unlike OPS-001's ship, which found TEA-02 stale at `IN_PROGRESS`. **The `copyDebt` ratchet reaches zero here**, which `ui-language.json`'s own header names as the proof the sweep is complete; from now on the only way that list can change is by growing, which is the failure mode. **`## ARCHIVE (last 20)` displaced `TEA-02`**, the second row pushed out of a window that first filled one ship ago; its content is reproduced under the table on ADM-06's convention, and the question of whether the heading grows or something else becomes the permanent record is now two rows old and still the operator's. |
| 2026-09-07T02:05:00Z | UIE-01 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | `size_estimate` S and `size` M **DISAGREE, and this is the first ticket on this board where they do**. ADR-012 is the decision that lets it proceed rather than routing back, and its own *What this decision does not change* names this exact case — *"lets an S-estimated ticket that designs out to M proceed"*. The whole gap is the `.woff2` files that `public/fonts/**` resolves to: five source files is S, eight is M, and an estimate that counts SURFACES does not see font binaries. The shell predicted the shape at §2 before PLAN ran. Gate PASS. **The plan closed the ticket's own `requires_adr` question without needing the operator**, which the shell required it either to answer or to stop on: of the three ways to get a font, a CDN `@import` is a runtime third party under `integrations.md:42` and `@fontsource/*` is an npm dependency that R9 fails without an ADR, so it specified self-hosted `@font-face` — the only path on which `requires_adr: false` stays true. **`invariants_touched: []` was reached STRUCTURALLY rather than by inspecting intent**, which is the circularity `invariants.md` warns about: every file in §7 sits above the seam and neither route imports the Supabase client, which RULE-02's lint rule would refuse. |
| 2026-09-07T02:27:00Z | UIE-01 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **SEVENTH TICKET RUNNING WHERE THIS TRANSITION WAS NEVER WRITTEN TO `ticket.yaml`**, and the second in a row. ADM-04's row named the cause, ADM-05's watched it recur, ADM-06's said prediction had stopped being informative, OPS-002's narrowed the description — and this ticket produced **the most careful refusal yet**: the `developer` left `gates.plan` false ON PURPOSE and wrote why, that flipping another stage's gate from `/implement` would record a DoR evaluation that never happened. **That reasoning is right and its premise is wrong, which is the whole finding.** The evaluation HAD happened, at 09:27 +07, between `/plan` and `/implement`. What is missing is a WRITER for a grade already given — not the grade, and not anybody's willingness to give it. **Five tickets have now recorded this and it still has no ticket of its own.** |
| 2026-09-07T02:35:00Z | UIE-01 | READY | IN_PROGRESS | developer | 0 | — | — | Seven of seven `allowed_paths` used. No consultation and no amendment. **The first ticket in this product to ship no capability, no behaviour change and no test file at all** — OPS-001 and OPS-002 shipped no capability but rewrote strings the suites assert on; this one changes appearance only. **`public/fonts/**` was written as a GLOB rather than an enumeration, deliberately**, because the exact filenames depend on which subsets the Developer can obtain and a wrong enumeration would have blocked the ticket at the guard over a naming detail. Six `.woff2` files landed under it — Nunito 400/700 and Baloo 2 700, each in latin and vietnamese subsets. **The Developer obtained them rather than reaching for a CDN**, which is what the plan's *Open questions* item 2 required and is the act that makes `requires_adr: false` true rather than merely unchanged. |
| 2026-09-07T02:52:00Z | UIE-01 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | **FAIL on R5, one finding, routed to `developer`** (`.ai/01-operating-model.md:144`). `01-plan.md:461-462` specifies the card's vertical rhythm as fields → 24px → error → 24px → button. The first four values shipped exactly; the last two did not — the error paragraph and the button were ordinary children of the form separated only by `FIELD_STACK`'s `gap-4`, 16px at both points. **What makes it a REVIEW finding rather than a nitpick is the second half:** the deviation was undeclared, and `03-impl-log.md` §4.3's contract row restated the plan's rhythm with those two values REMOVED — a contract row that agrees with the code by having been rewritten to match it is the one thing this gate exists to catch. |
| 2026-09-07T03:02:00Z | UIE-01 | REWORK | REVIEW | developer | 1 | — | — | **RULE-08 charges this cycle, unlike OPS-002's amendment, and the difference is where the defect was.** OPS-002's plan was wrong and `tech-lead-design` fixed it, so no rework. Here the plan was right and the implementation did not match it, so `rework_count: 0 → 1`. **The `developer` set the count and deliberately did NOT touch `state`**, on the same reasoning that left `gates.plan` alone: REVIEW → REWORK is `orchestrator`'s transition and no orchestrator ran between the two commands. **The fix is better than a minimal one in two respects worth recording**: it went onto the shared spacing constants rather than onto the two screens, so they cannot drift on a spacing value, and it wrote the 16 + 8 arithmetic into `AuthCard.tsx:119-121,128-134,151-152` so the next reader does not re-derive it. The elision in the impl log is acknowledged in place rather than edited away. |
| 2026-09-07T03:55:14Z | UIE-01 | REVIEW | REVIEW | tech-lead-review | 1 | — | — | **Review pass 2: R1-R9 all PASS**, and pass 1 is recorded in the same report's § *Review pass 1* rather than overwritten, so the cycle stays legible against the code that answered it. Rhythm measured 24/24/24/24. **All four commands exit 0** — `tsc --noEmit`, `eslint .`, `vitest run` 186 tests in 10 files, `playwright test` **165 tests, all pass, including the `seam-guard` project**. AC-18 was re-run rather than re-read: both acceptance suites pass UNEDITED, which is the frozen-selector constraint holding and the reason this ticket is M rather than L. **R7's structural claim is the one worth keeping**: every file in the diff sits above the seam — `AuthCard.tsx` imports `react` and `react-router-dom` only, both routes reach the datastore through `@/lib/data`, `App.tsx`'s diff is one `className`, `index.css` is a stylesheet — so `invariants_touched: []` rests on what the files import rather than on what they appear to do. |
| 2026-09-07T04:01:23Z | UIE-01 | REVIEW | DONE | orchestrator | 1 | — | — | One rework cycle, no consultation, no amendment. Branch base was `93b2a05`, the current `origin/main`, so **no merge was needed**. **Definition of Done item 3 was not re-run at `/ship`** — the operator instructed step 1 to be skipped, as on the five ships before it — **but unlike OPS-002 this one has a green end-to-end run behind it**: REVIEW pass 2 ran all four commands on this tree with nothing committed since and `origin/main` unmoved under it. The gap OPS-002's row named is not repeated here. **`.ai/registry/features.md` was written**: `UIE-01` PLANNED → DONE, the first row in the `UIE` group to reach it and the first registry write since OPS-001's ship. **One of the row's two `TODO(project):` markers is discharged and one is not, and the distinction matters more than either**: the font question was closed by a decision PLAN was entitled to make, while `§ Colour` and `§ Type` in `.ai/standards/ui-design-system.md` are STILL bare stubs — so `src/index.css` is now the product's de-facto palette and type scale with no document behind it, and **UIE-02 is next on the board with nothing to conform to**. That is human plane under RULE-01 and belongs to `/thuki`, not to a ticket. **`## ARCHIVE (last 20)` displaced `TEA-03`, the third row out**, and the interval between displacements is now one ship. |
| 2026-09-07T04:15:03Z | UIE-02 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | Gate PASS. `size_estimate` M and `size` M **AGREE** at eleven of M's ceiling of twelve, and the estimate was made before the count — so ADR-012 is not engaged, unlike UIE-01 one row up. **The plan's most valuable act was re-measuring the shell rather than trusting it.** Four statements written at triage on 2026-09-05 had gone stale by 2026-09-07 and PLAN corrected each IN PLACE rather than deleting it: ADM-04, ADM-05 and ADM-06 are `DONE`, so §9.2's premise — *no route is registered, no component exists* — is gone, and **its conclusion survives only for a different reason**, that ADM-04 shipped its own link and that link relocates into the sidebar; there are TWELVE `home-*` ids, not eleven; FIFTEEN spec files address one, not twelve, and §5 named `cal-03-team-entries.spec.ts`, **a file that does not exist**; and there are FIVE membership guards on routes, not four, with every line number in §3 stale. **`invariants_touched: []` was reached structurally**: the whole ticket makes ONE seam call, `seam.listMembers()`, which `MemberList.tsx:54` already makes under a policy admitting both roles. |
| 2026-09-07T05:07:00Z | UIE-02 | PLAN | PLAN | tech-lead-design | 0 | — | — | **PLAN rework 1, and it cost no `rework_count` because the defect was the plan's own** (RULE-08). The 11:15 issue asserted ZERO spec files in scope. That was wrong in exactly one place and the plan found it by measuring: the sidebar roster names every active member on EVERY screen, and `tests/e2e/cal-03-admin-edit-entry.spec.ts:493` asserts **page-wide** that an entry owner's display name is absent from a refusal screen. **The fix was to re-scope the assertion to the content pane, not to hide the roster** — its stated intent, *"a refusal that said what it was withholding would be the read it is refusing"*, is preserved exactly, because the roster is a read that *Read the member list* grants a member outright. `allowed_paths` twelve → thirteen, still M. **It is the only spec file this ticket ever touches**, and the twelve `home-*` ids relocating UNRENAMED is what keeps the other fourteen passing unedited. |
| 2026-09-07T05:30:00Z | UIE-02 | READY | IN_PROGRESS | developer | 0 | — | — | Thirteen of thirteen `allowed_paths` used. **`src/routes/Home.tsx` is DELETED — the first file this product has removed at a ticket** — because the nav hub becomes the shell and a second copy of any `home-*` id would double under Playwright's strict mode. All four commands exit 0 at this cycle: `tsc`, `eslint`, `vitest run` 10 files / **186 tests**, `playwright test --workers=1` **165 passed**. **165 = 165 against the pre-ticket baseline, measured by stashing the ticket and re-running** rather than asserted — and cycle 1's tree is recorded at 142/23 so the amendment's effect stays legible. **Deviation 2 is worth keeping**: §5 forbids `src/lib/period.ts` a data import, so its day arithmetic and leap rule are written against the standard library; `grep -c "^import"` on that file returns **0**. |
| 2026-09-07T06:45:35Z | UIE-02 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | **FAIL, two findings, and they route to different owners — which is the row's point.** Finding 1 is §4.7's gutter: the auth card's 32px side margin was not held at 320px and 360px. R5 implementable, `developer`'s, and it is what charges `rework_count`. **Finding 2 was a contract question and the Developer routed it rather than fixing it**: AC-12 gives three literal `/week` addresses that step ±7 days from the anchor day in the URL, while `src/lib/period.ts:307-314` steps ±7 from the Monday of the anchor's week. **Both reach the same seven-day windows in the same order** and `WeekView` resolves any day to the same Monday, so `shell-period-anchor` reads identically under either — a disagreement about the ADDRESS only. `99-questions.md` carries it with both forms worked out. |
| 2026-09-07T07:00:28Z | UIE-02 | REWORK | REVIEW | developer | 1 | — | — | **RULE-08 charges this cycle: the plan was right and the implementation did not match it.** `rework_count: 0 → 1`, set by the `developer`, who **deliberately did not touch `state`** — REVIEW → REWORK is `orchestrator`'s and none ran between the FAIL and the rework, the same refusal UIE-01's developer made about `gates.plan`. The gutter is fixed at `src/App.tsx:122-140` and re-measured at both widths. **`vitest run` was NOT re-run at this cycle** — `tsc`, `eslint` and `playwright` were, the last at 165 level with cycle 2. `tech-lead-design` closed finding 2 at 14:20 by amending the AC to the code's addresses, since both satisfy the criterion's own second clause. |
| 2026-09-07T07:42:19Z | UIE-02 | REVIEW | REVIEW | tech-lead-review | 1 | — | — | **Review pass 2: R1-R8 all PASS**, each with a `file:line` or a command run in that session, and pass 1 is superseded rather than deleted. **R6 was proven live rather than read**: `tests/e2e/tea-05-sign-in.spec.ts:143-158` signs in as each role and asserts the admin affordances visible for one and `toHaveCount(0)` for the other, and it passes — the four admin-only items sit under the one `member.role === "admin"` condition `Home.tsx` used, MOVED rather than rewritten. **R1 counted all 27 selector ids to exactly one definition site each**, which is the claim the whole no-spec-changes design rests on. `tsc`, `eslint` and `playwright test --workers=1` (165 passed) were run in this session; **`vitest run` was not**. |
| 2026-09-07T07:46:37Z | UIE-02 | REVIEW | DONE | orchestrator | 1 | — | — | One PLAN rework (uncharged), one REVIEW rework (charged), one consultation, two amendments. Branch base was `eab2cbd`, the current `origin/main`, so **no merge was needed**. **Definition of Done item 3 is covered in three of four commands and the gap is named rather than absorbed.** The operator instructed step 1 to be skipped, as on the six ships before it. `tsc --noEmit`, `eslint .` and `playwright test --workers=1` (165 passed) were all run against the FINAL tree at review pass 2. **`pnpm exec vitest run` was last run at `/implement` cycle 2 — before the cycle-3 rework — and at no point after it.** The rework touched `src/App.tsx`'s gutter and the route table, which no unit suite exercises, so the exposure is small; it is recorded because *small* is a judgement and *not re-run* is a fact. **This is the third distinct shape of the same item-3 gap in three ships**: OPS-002 had no `playwright` at any stage, UIE-01 had all four, UIE-02 has all four but one of them one cycle stale. **`features.md` written**: `UIE-02` PLANNED → DONE. **`## ARCHIVE (last 20)` displaced `TEA-04`, the fourth row out and the third in three ships.** |
| 2026-09-07T08:01:57Z | UIE-03 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | Gate PASS. `size_estimate` M and `size` M agree at TWELVE — **M's ceiling exactly, no headroom** — and the plan says the agreement is only real because the estimate was formed AFTER section 0's measurement rather than from §2 of the shell, which predicts ten. **PLAN OVERRODE §7 OF ITS OWN SHELL, on the authority of a later document, and this is the row's finding.** §2 and §3 assume UIE-02 relocated the screens' selector ids onto the shell. **It did not.** UIE-02 shipped `shell-period-*` and left `data-period-kind` on its anchor rather than `data-week-start` / `data-month` / `data-year` — deliberately, because while both copies existed a reused id resolved to two nodes and Playwright's strict mode FAILS rather than choosing one. **UIE-02's `01-plan.md` § 4.8 handed the decision here in as many words**, so `src/components/TopBar.tsx` and `src/lib/period.ts` came into scope against §7, with the grant narrowed to test ids and the data attributes beside them. Without it, deleting four screen headers destroys **83 spec references**. **`tests/e2e/adm-01-threshold.spec.ts` is named specifically to be EXCLUDED** — it references two touched ids and both survive — so a reader who counts seven files referencing a touched id knows why six were edited. |
| 2026-09-07T08:07:00Z | UIE-03 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **NINTH TICKET RUNNING WHERE THIS TRANSITION WAS NEVER WRITTEN TO `ticket.yaml`, AND THIS ROW CORRECTS HOW THE PREVIOUS EIGHT DESCRIBED IT.** The `developer` wrote that the row read BACKLOG on arrival because *"PLAN did not carry the value into this file — the gap is PLAN's"*. **IT IS NOT PLAN'S.** `.ai/01-operating-model.md:285` puts PLAN -> READY inside the ORCHESTRATOR's loop: read the plan gate, evaluate DoR, write the state. `/plan` writing `state: READY` itself would be a stage grading its own Definition of Ready, which is the one thing the gate placement exists to prevent. **What is missing is a COMMAND that runs that loop step** — `/next-ticket` grades it and writes no file by design, and `/ship` is where it all lands at the end. Six shells have now blamed a different owner for the same hole, which is a good reason it has never been closed. **THE PREVIOUS RUN OF THIS COMMAND WAS THE FIRST TO REPORT NOTHING RUNNABLE**: UIE-03 could not be planned until PR #64 merged, because `origin/main` carried no shell and `/plan` cuts from `origin/main`. Item 3 passed on the letter — UIE-02's `ticket.yaml` read `DONE`, written by `/ship` BEFORE the merge by that command's own design — while the thing item 3 protects was not yet true. **No field on this board carries "merged".** |
| 2026-09-07T08:12:00Z | UIE-03 | READY | IN_PROGRESS | developer | 0 | — | — | Twelve of twelve `allowed_paths` used. **No consultation, no amendment, no deviation routed** — and six spec files edited, the most of any ticket in this product. Each loses exactly one navigation step: the screens' own headers are deleted and the shell already carries the equivalent, so the specs stop clicking a control that no longer exists and click the shell's instead. All four commands exit 0 at this cycle: `tsc`, `eslint` (which found three now-unused imports, removed), `vitest run` 186 in 10 files, `playwright test` **165 passed — the whole suite**. **AC-13's evidence is that 83 references to relocated ids pass with NO edit to the files holding them**, which is the ticket's own proof that it moved chrome and changed no behaviour. |
| 2026-09-07T08:33:12Z | UIE-03 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | **R1-R8 all PASS on the FIRST pass — the first ticket since ADM-06 to need no rework, no consultation and no amendment.** Isolated dispatch, fresh session, files only; `chat_before_verdict: none` is true rather than asserted. **All four commands were run in that session on the final tree** rather than read out of `03-impl-log.md`, and the report says why in its own words: *"A verification table copied from the artifact being judged is not evidence."* `tsc --noEmit` 0, `eslint .` 0 (which is also RULE-02's and § Language's enforcement, so AC-15 rides the same run), `vitest run` 186 in 10 files, `playwright test` **165 passed in 23.8s**. |
| 2026-09-07T08:39:22Z | UIE-03 | REVIEW | DONE | orchestrator | 0 | — | — | No rework, no consultation, no amendment. Branch base was `61825ae`, the current `origin/main`, so **no merge was needed** — and unlike the previous attempt at this chain, the base was VERIFIED to carry the shell before PLAN ran. **Definition of Done item 3 is fully covered and this is the cleanest of the four ships to record it.** Step 1 was skipped on the operator's instruction, as on the seven ships before it, but REVIEW ran ALL FOUR commands on the final tree with nothing committed since and `origin/main` unmoved: OPS-002 had no `playwright` at any stage, UIE-02 had `vitest` one cycle stale, UIE-01 and this one have all four against the tree that ships. **`features.md` written**: `UIE-03` PLANNED → DONE. **`## ARCHIVE (last 20)` displaced `BUG-001`, the fifth row out and the COSTLIEST so far** — BUG-001 is the pair of pull requests ADR-023 exists because of, and this table was the only file naming both #27 and #28. **ONE ROW IS LEFT ON THE BOARD.** After `UIE-04` the BACKLOG table is empty, which is a state nothing in `backlog.md` describes; better decided by a human than discovered at a `/next-ticket` with nothing to report. |
| 2026-09-07T08:54:51Z | UIE-04 | BACKLOG | PLAN | tech-lead-design | 0 | — | — | Gate PASS. `size_estimate` S and `size` S agree at **ONE file outside the ticket folder**, so ADR-012 never engages. `ticket.yaml` §2 predicted two files, possibly four, and warned the §3 choice would move the cost by more than the file count suggests — **it did, downward**: all three candidate spec files measured out of scope, because `cal-07` addresses no week selector at all and `cal-05` and `cal-08` reach this screen only through ids, data attributes and DOM order, every one of which the chosen layout preserves. **`invariants_touched: [INV-04]` — THE ONLY NON-EMPTY VALUE IN THE `UIE` GROUP**, and exactly the exception §6 predicted against ADR-028's note that `[]` would be the answer on almost every UIE row. **PLAN REFUSED EVEN TRIAGE'S OWN RECOMMENDATION AND THIS IS THE ROW'S FINDING.** §4 offered the footer the day's own chip count as a safe substitute for `n/total vắng` — no arithmetic, no roster read, no team call. **It is not safe:** a day holding one full-day and two half-day entries has THREE chips and an absence count of TWO, so the footer would contradict the month grid for the same date. **That is the second definition INV-04 forbids, reached without ever opening `absence.ts`** — the exact shape CAL-04's row spent its length warning about. AC-13 adds no count, so §4's stop-and-ask never fired and no amendment is owed to CAL-05's registry row. **Of §3's three options it took Option 3's breakpoint with Option 1's overflow behaviour** and refused Option 2 outright, which would have deleted `week-row-note` and `week-row-approver` from the resting view — CAL-05's AC-6 and AC-7. |
| 2026-09-07T09:02:00Z | UIE-04 | PLAN | READY | orchestrator | 0 | — | — | Full DoR, six of six, graded at `/next-ticket`. **TENTH AND LAST TICKET TO RUN WITH THIS TRANSITION UNWRITTEN, AND THIS SHELL FINALLY STATES THE MECHANISM CORRECTLY.** Nine before it laid the gap at PLAN's door; this one cites `.claude/commands/plan.md:114` and gets it right — `/plan` sets neither `state` nor `gates.plan` **because that file leaves both to the orchestrator**, and no orchestrator run happens between PLAN and `/implement`. It is a loop step at `.ai/01-operating-model.md:285` that **NO COMMAND RUNS**: `/next-ticket` grades and writes no file by design, `/ship` transcribes both gates at the end, and in between the fields sit stale. **Ten tickets, six of them blaming a different owner, and no ticket was ever issued for it.** With the board now empty it is one of the four things `backlog.md` records as waiting for `/triage`, and it is `/thuki`'s. |
| 2026-09-07T09:11:05Z | UIE-04 | READY | IN_PROGRESS | developer | 0 | — | — | One of one `allowed_paths` used. No consultation, no amendment, no deviation. **All four commands exit 0 in this session on this tree**: `tsc --noEmit`, `eslint .` (which is also the § Language rule the mascot's new sentence had to pass), `vitest run` **186 tests in 10 files**, `playwright test` **165 tests with NO test file edited** — AC-14. `vite build` was run additionally, not as a gate, to read the generated stylesheet: **two facts about a dependency `tech-stack.md` marks as past reliable recall were verified by reading the machine rather than by recall**, which is what that list exists to force. |
| 2026-09-07T09:17:25Z | UIE-04 | IN_PROGRESS | REVIEW | tech-lead-review | 0 | — | — | **R1-R8 all PASS on the first pass — the second consecutive ticket needing no rework, no consultation and no amendment**, after UIE-03 broke a run of five that each needed at least one. `tsc --noEmit` and `eslint .` re-run in the review session, and `playwright test` **165 passed** with `week-row-note` at `:473` and `week-row-approver` at `:483` both still present in the resting view — which is Option 2's refusal verified against the code rather than taken from the plan. The reviewer also read `dist/assets/index-*.css`, the built stylesheet, for two utility facts rather than asserting them from source. |
| 2026-09-07T09:23:25Z | UIE-04 | REVIEW | DONE | orchestrator | 0 | — | — | No rework, no consultation, no amendment. Branch base was `f606129`, the current `origin/main`, so **no merge was needed**. **Definition of Done item 3 is covered on the shipping tree.** Step 1 was skipped on the operator's instruction, as on the eight ships before it. REVIEW re-ran `tsc`, `eslint` and `playwright` (165 passed); `vitest run` (186 in 10 files) is `/implement`'s at 16:11 — and **unlike UIE-02, nothing changed after it**: `rework_count: 0`, one implement cycle, one review pass, no commit between them. The unit evidence is against the tree that ships. **`features.md` written**: `UIE-04` PLANNED → DONE, the fourth and last `UIE` row. **`## ARCHIVE (last 20)` displaced `TEA-05`, the sixth row out and the fifth in five ships** — the window has now displaced a row at every ship since it filled, and the question of whether the heading grows has been asked five times and answered none. ***THIS SHIP EMPTIES THE BOARD.*** Twenty-six tickets, `## BACKLOG` an empty table for the first time, and nothing in the loop produces work on its own — `backlog.md` now carries what reopens it and the four things already written down and waiting for `/triage`. |

## Targets

These are the numbers the model is judged by, and one of them is a revert condition.

| Metric | Target | Read from |
|---|---|---|
| Amendment rate | 60% or above; **below 40% over ten consecutive tickets reverts ADR-001** | `resulted_in_amendment: true` in `consulted` blocks |
| Rework cycles per ticket | 1 or fewer | `rework_count` at DONE |
| Escalations | any is worth reading; a second on the same invariant is a modelling problem | rows whose `to` is `ESCALATED` |
| Chat budget pressure | a pair that repeatedly reaches 6 is negotiating, not clarifying | `chat_budget` in `ticket.yaml` |

## Escalations

One row per escalation, kept separately because they halt rather than queue.

| ts | ticket | invariant or check | decided by | outcome |
|----|--------|--------------------|------------|---------|

Empty because no ticket has run.
